Epsilon_Rot { ; Wayne Kiely
;epsiloncross test rotated 45 degrees
    z=c=pixel:
    z=z*z+c
    r=real(z), i=imag(z)
    if ((((r-i)>-0.01) && ((r-i)<0.01)) || (((r+i)>-0.01) && ((r+i)<0.01)))
      z=1E6            ;forces escape
    endif
    |z| < 4
}
