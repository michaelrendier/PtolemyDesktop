comment {
  Carr3400.ufm Version 1.6
  Ultra Fractal conversions of Robert Carr's formulas 3400 - 3499
  by Erik Reckase, February 24, 2000

  History:  Version 1.6 - minor optimization tweaks + cleanup

            Version 1.5 - better hints + fix over-optimization
                          optimization tweeks + long line trunc

            Version 1.4 - major optimization overhaul
                          method = multipass

            Version 1.3 - Removed unused whitesq code
                          Optimized bailout condition
                          Reduced iteration tests

            Version 1.2 - Made the following optimizations

                          Carr3417,3459-3460,3464
                              3465-3466,3469-3471  : flip(flip) removed

                          Carr3401,3412,3417-3418
                              3426,3428-3430,3452
                              3459-3460,3464-3466
                              3486-3487,3491-3492  : conj(conj) removed

                          Carr3407,3413,3421-3424
                              3427,3431,3446,3472
                              3473-3478,3481-3482
                              3491,3497-3499       : abs(conj) -> abs()


            Version 1.1 - Modified all whitesq code to eliminate warns

            Version 1.0 - Initial Release
                          Original formulas pulled from 43hCarr.frm
                                                        43iCarr.frm
                                                        43jCarr.frm
                          Carr3400-3499            : pixel fix
                          Carr3400-3499            : removed unnecessary code
                          Carr3431                 : variables not used, removed
                          Carr3436                 : | | fix
                          Carr3400,3412,3447,3490  : cosxx fix
}

Carr3400 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b6=conj(.025/pixel2), b7=flip(.01/pixel2)
  b8=cabs(.002/pixel2+cabs(.002/pixel2))
  b3=exp(.0045/pixel2)/conj(cos(.1-pixel2))-conj(.01/pixel2)
  b2=(b8-b6-b7)/.65-.002/pixel2
  b10a=pixel2-flip(.01/pixel2)-tanh(.01/pixel2)-conj(.01/pixel2)
  b10=conj(pixel2/3)*sinh(pixel2)^2*tanh(pixel2) \
      *conj(abs(pixel2/2))
  z=conj(b10a*b10)^2-b2*.5+b6+b7-.02*b3+.075
  IF (@p3==0)
    z = z^.8456
  ELSEIF (@p3==2)
    IF (whitesq)
      z = z^.8456
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7602073214,.080996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3400"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3401 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=pixel2^3*cabs(pixel2^4)-flip(.01/pixel2)-conj(.01/pixel2)-.1
  IF (@p3==0)
    z = z^.8456
  ELSEIF (@p3==2)
    IF (whitesq)
      z = z^.8456
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.080996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3401"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3402 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=|pixel2|^4-conj(.15/pixel2)-flip(.15/pixel2)+.23
  IF (@p3==0)
    z = z^.8456
  ELSEIF (@p3==2)
    IF (whitesq)
      z = z^.8456
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.084496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3402"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3403 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=|pixel2|*pixel2^2*abs(pixel2)^4-conj(.15/pixel2) \
    -flip(.015/pixel2)-.06/pixel2+.23
  IF (@p3==1)
    z = z^.8456
  ELSEIF (@p3==2)
    IF (whitesq)
      z = z^.8456
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.084496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3403"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3404 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b8=cabs(.002/pixel2+cabs(.002/pixel2))
  b9=abs(pixel2^3)^4*conj(pixel2)^4*pixel2/2
  b3=(.5+pixel2/2)^7-conj(.2/pixel2)-flip(.2/pixel2)+.3
  z=b9-conj(.175/pixel2)-flip(.15/pixel2)-b8-.01/b3 \
    -conj(.01/b3)+.29
  IF (@p3==1)
    z = z^.8456
  ELSEIF (@p3==2)
    IF (whitesq)
      z = z^.8456
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.745634221,-.122236621)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3404"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3405 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=|pixel2|^2*pixel2^3*sqrt(pixel2^20)-conj(.1/pixel2)+.3
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7610073214,.084496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3405"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3406 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=cabs(pixel2^5)*conj(pixel2)^2*|pixel2^10|-conj(.1/pixel2)+.3
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7610073214,.084496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3406"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3407 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=conj(.1/pixel2-flip(.01/pixel2))
  b5=cabs(pixel2^4)*|pixel2|^3*|pixel2^10|-b3^3+.365
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.37223341,-.299137213)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3407"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3408 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=conj(pixel2^3)*sin(sin(pixel2))-conj(.1/pixel2)
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7456208989,-.132750082)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3408"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3409 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=conj(conj(pixel2^3.874)*sin(conj(pixel2^3.8))) \
     -conj(.1/pixel2-cabs(.1/pixel2))+.2
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.085496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3409"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3410 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=pixel2^6/64*abs(pixel2^2)-conj(.10/pixel2)+.25
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.085496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3410"
  periodicity = 0
  maxiter = 700
  magn = .8
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3411 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=(pixel2/2-conj(.1/pixel2))^6*abs(pixel2^2)*conj(pixel2^2) \
     -conj(.1/pixel2)+.25
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.085496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3411"
  periodicity = 0
  maxiter = 700
  magn = .8
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3412 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  d=sqrt(1/pixel2-conj(cos(1/pixel2)))
  b5=(pixel2^3-conj(.1/pixel2))^6*cabs(pixel2/2)*pixel2^2 \
     -conj(.1/pixel2)+.25
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  k = (-.7612073214,.085496112)+flip(|d/10*pixel2|)
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+k
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3412"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3413 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=conj(pixel2/3)^2-pixel2/2
  b5=b3*abs(pixel2^7)/tanh(.6/pixel2)-conj(.05/pixel2)-.13
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084296112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3413"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3414 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=|pixel2|^7-1/log(.001/pixel2)-cos(.01/pixel2+1.25)-\
     conj(.1/pixel2)
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3414"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3415 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=pixel2-conj(.1/pixel2-flip(.01/pixel2))
  b5=(conj(b3/2)*abs(b3)*b3^3-conj(.01/b3))/tanh(.3/pixel2)+.35
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.085096112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3415"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3416 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=(1.224*cabs(pixel2^5)-flip(conj(pixel2)))
  b5=(conj(b3/2)*abs(b3)*b3^3-conj(.01/b3))/tanh(.6/pixel2)+.37
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084796112)
  iter=iter+1
bailout:
  imag(flip(z))<=bailout
default:
  title = "Carr 3416"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,-.15)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3417 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=(pixel2^3+.2/pixel2)^2
  b5=abs(pixel2)*cabs(b3)*|pixel2/2|*(pixel2/2)^4 \
     -conj(.11/pixel2)-flip(.07/pixel2)+.38
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.085396112)
  iter=iter+1
bailout:
  |z^6|<=bailout
default:
  title = "Carr 3417"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3418 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=(pixel2^3+.2/pixel2)^2
  b5=|pixel2|*cabs(b3)*pixel2/2*flip(conj(pixel2/2))^5 \
     -conj(.11/pixel2)-flip(.07/pixel2)+.38
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.085396112)
  iter=iter+1
bailout:
  |z^6|<=bailout
default:
  title = "Carr 3418"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3419 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=flip(conj(pixel2-flip(.01/pixel2)))*(conj(pixel2^3 \
     -conj(.1/pixel2)))
  b5=b3-tanh(.1/pixel2)+sinh(.165/pixel2)-flip(.1/pixel2)+.25
  IF (@p3==1)
    z = b5^.8456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.8456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.085396112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3419"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3420 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=cabs(pixel2/2)-pixel2^5+.07/pixel2+.2
  IF (@p3==1)
    z = b5^.7456
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.7456
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7614773214,.086456112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3420"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3421 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=conj(pixel2)^3*pixel2/12*(cabs(pixel2^3)^85*|pixel2|^5) \
     -.1/pixel2+.38
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.745637089,-.1563332)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3421"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3422 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=conj(pixel2)^3*pixel2/12*(cabs(pixel2^3)^85*|pixel2|^5) \
     -.1/pixel2+.38
  b3=conj(.1/pixel2-flip(.01/pixel2)), b1=conj(pixel2^3)*|pixel2|
  b5=b2*(b1+flip(asin(.01/pixel2))-conj(asin(.1/pixel2))+b3)+.3
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(-.744037089,-.1192332)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3422"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3423 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), op = .1/pixel2
  b2=conj(pixel2)^3*pixel2/12*cabs(pixel2^3)^85*cabs(pixel2)^5 \
     -op+.38
  b3=conj(op-flip(.01/pixel2)), b1=conj(pixel2^3)*|pixel2|
  b5=b2*(b1+(flip(asin(.01/pixel2))-conj(asin(op)))+b3) \
     /tanh(.5/pixel2)+.3
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(-.743337089,-.1191932)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3423"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3424 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), op = .1/pixel2
  b2=conj(pixel2)^3*pixel2/12*cabs(pixel2^3)^85*cabs(pixel2)^5 \
     -op+.38
  b3=conj(op-flip(.01/pixel2))
  b1=conj(pixel2^3)*pixel2^2-conj(op)
  b5=b2*(b1+flip(asin(.01/pixel2))-conj(asin(op))+b3) \
     /tanh(.2/pixel2)+.45
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(-.743337089,-.1186932)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3424"
  periodicity = 0
  maxiter = 700
  magn = 1.7
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3425 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=|pixel2|^7-1/log(.001/pixel2)-cos(.01/pixel2+1.25) \
     -conj(.1/pixel2)
  b3=abs(conj(pixel2)*flip(pixel2))
  b5=conj(pixel2^3)*b3*b2/6 \
     -conj(.1/pixel2-flip(.015/pixel2-conj(.01/b3)))-.15
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.37223341,-.312137213)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3425"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3426 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=|pixel2|^7-1/log(.001/pixel2)-cos(.01/pixel2+1.25)\
     -conj(.1/pixel2)
  b3=pixel2^3-.1/pixel2-cotan(cotan(1/pixel2))
  b1=conj(pixel2^3)*cabs(b3)/6*flip(b2)
  b5=b1-conj(.1/pixel2-flip(.02/pixel2-conj(.01/b3))) \
     -conj(.08/pixel2)-0.1
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(.37183341,-.312637213)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3426"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3427 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=|pixel2|*pixel2/6
  b3=pixel2/3*flip(conj(pixel2^5))*abs(pixel2)
  b5=conj(b3*(b2-.1/pixel2))^3/(b3-b2)-conj(.0125/pixel2 \
     -flip(.005/pixel2))-.1/pixel2
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(.37383341,-.312337213)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3427"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3428 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.1/pixel2, b6=conj(.075/pixel2), b7=flip(.01/pixel2)
  b8=sin(.003/pixel2-b7)
  b3=abs(pixel2^2)*conj(flip(pixel2^3))-conj(b1-b7)
  b5=conj(b3/3)*pixel2^3*cabs(pixel2-b1)^4-b7-b6*b8+b1-.2
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(.37470341887,-.3123372136)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3428"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3429 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b6=conj(.05/pixel2), b7=flip(.01/pixel2)
  b8=sin(.003/pixel2-b7)
  b3=abs(pixel2^2)*conj(flip(pixel2^3))-conj(.1/pixel2-b7)
  b5=conj(b3/3)*pixel2^3*cabs(pixel2-.05/pixel2)^4-b7-b6*b8 \
     +.12/pixel2-0.3
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(.22370345,.53229573)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3429"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3430 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b6=conj(.05/pixel2), b7=flip(.01/pixel2)
  b8=sin(.003/pixel2-b7)
  b3=abs(pixel2/2)*conj(flip(pixel2))-conj(.07/pixel2 \
     -flip(.007/pixel2))
  b5=|b3/3|*pixel2^2*abs(pixel2-.05/pixel2)^2-b7-b6*b8 \
     +.12/pixel2-.2
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z*z+(.22490345,.53179573)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3430"
  periodicity = 0
  maxiter = 700
  magn = .9
  center = (0,-.08)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3431 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=flip(conj(pixel2))*abs(pixel2)*pixel2-conj(.0125/pixel2\
     -flip(.005/pixel2))
  b5=2.25*b3^2-conj(.001/b3-flip(.001/b3))+.0124+.1/pixel2
  float bailout=4
  IF (@p4==1)
    z = b5^.845
  ELSEIF (@p4==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
loop:
  z=z*z+(-.7882322074096,-.1502158760371)
bailout:
  |z|<=bailout
default:
  title = "Carr 3431"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  param p4
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3432 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b10=conj(pixel2)+abs(.1-pixel2)*cabs(.15-pixel2^2)
  b3=pixel2^8-flip(.2/pixel2)
  b5=b10^4*b3+.29
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.0335,.695)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3432"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,.16)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3433 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=|pixel2|^5*conj(pixel2)^6/8-conj(.15/pixel2)^2 \
     -flip(.12/pixel2)+.23
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3433"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3434 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=flip(conj(pixel2^6)*|pixel2^5|)/conj(.3/pixel2) \
     *conj(pixel2-.01/pixel2)-.11/pixel2-.17
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7620073214,.085696112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3434"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3435 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=cabs(pixel2^5)-cos(.066/pixel2)^3
  b2=conj(pixel2-.01/pixel2)*flip(pixel2+.1/pixel2)
  b5=flip(|pixel2|^6*b3)/conj(0.3/pixel2)*b2/2 \
     -.125/pixel2-.17
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7615073214,.086096112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3435"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3436 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  temp2 = |.066/pixel2|
  b3=cabs(pixel2^5)-acosh(temp2)^5
  b2=conj(pixel2-.01/pixel2)*flip(pixel2+.1/pixel2)
  b5=flip(|pixel2|^6*b3)/conj(0.3/pixel2)*(b2+pixel2) \
     -.125/pixel2-.24
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7615073214,.086096112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3436"
  periodicity = 0
  maxiter = 700
  magn = 1.7
  center = (0,.07)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3437 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b6=-2*flip(imag(.1/pixel2))
  b2=acos(conj(pixel2-.01/pixel2))*flip(pixel2+.1/pixel2)
  b5=b2*conj(pixel2^8)*cabs(pixel2+.125)^7-.15/pixel2-b6-.05
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7910322074096,-.1446158760371)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3437"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3438 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=|pixel2|^7-1/log(.001/pixel2)-cos(.01/pixel2+1.25) \
     -conj(.1/pixel2)
  b3=abs(conj(pixel2)*flip(pixel2))
  b5=conj(pixel2^3)*b2^2*b3/3-conj(.01/b3)-.035
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7910322074096,-.1446158760371)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3438"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3439 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=|pixel2^7|-1/log(.001/pixel2)-cos(.01/pixel2+1.25) \
     -conj(.1/pixel2)
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3439"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3440 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=|pixel2^11|-conj(.1/pixel2)-flip(.15/pixel2)+.03/pixel2+.19
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3440"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3441 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.03/pixel2
  b5=|pixel2^7+b1|*pixel2^4-|pixel2^4|-flip(.1/pixel2)+b1+.16
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3441"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3442 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.03/pixel2
  b3=sqrt(|pixel2^4|-conj(.09/pixel2))
  b5=|pixel2^7+b1|*pixel2^4+b3-flip(.1/pixel2)+b1+.16
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3442"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3443 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.045/pixel2
  b2=sqrt(|pixel2^4|), b3=sqrt(|pixel2^4|-conj(.09/pixel2))
  b5=|pixel2^7+b1|*pixel2^5+b3+|b2/4|-flip(.1/pixel2)+b1+.158
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3443"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3444 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.045/pixel2
  b2=sqrt(|pixel2^4|), b3=.8/log(|pixel2^4|-conj(.09/pixel2))
  b5=|pixel2^7+b1|*pixel2^5+b3+|b2/4|-flip(.1/pixel2)+b1+.158
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3444"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3445 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b5=(|pixel2^8|-conj(.175/pixel2))/tanh(0.3/pixel2) \
     /(2*pixel2-.175)
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3445"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3446 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(.1-pixel2/3)*flip(cabs(pixel2/2-.3/pixel2)) \
     *(pixel2-cabs(.1/pixel2))
  b5=|b2^2|-conj(.035/pixel2-flip(.01/pixel2))-.023
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7633323214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3446"
  periodicity = 0
  maxiter = 700
  magn = .8
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3447 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), op = .002/pixel2
  b6=conj(.0425/pixel2),b7=flip(.007/pixel2),b8=cabs(op+cabs(op))
  b3=exp(.0045/pixel2)/conj(cos(.1-pixel2))-conj(.01/pixel2)
  b2=|b8/.65|-(b6+b7)/.65-op
  b10a=pixel2-flip(.01/pixel2)-tanh(.01/pixel2)-conj(.01/pixel2)
  b10=conj(pixel2/3)*sinh(pixel2)^2*tanh(pixel2) \
      *conj(abs(pixel2/2))
  b5=b10+conj(b10^.75)+asin(b10/2)+conj(|b10a|*b10)^2-b2*.5 \
     +b6+b7-.02*b3+.075
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7633323214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3447"
  periodicity = 0
  maxiter = 700
  magn = 1.1
  center = (0,.03)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3448 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=conj(pixel2/3)*sinh(pixel2)^2*tanh(pixel2)*conj(abs(pixel2/2))
  b5=b2+|pixel2|^4*conj(pixel2)^4-conj(.1/pixel2) \
     -flip(.015/pixel2)+.29
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = 0
  ENDIF
  z=z^2+(-.7633323214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3448"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,.03)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3449 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.000275/|pixel2|-real(.025/pixel2)
  b3=|pixel2|/6, b2=|pixel2^6|
  b5=b3*b2-(.1/pixel2)+b1-.06
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3449"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3450 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=asin(|.03/pixel2|)-|.025/pixel2|-real(.025/pixel2)
  b3=5*|pixel2|/48
  b2=|pixel2^6|-|pixel2|
  b5=b3*b2/2-.12/pixel2+b1^2+conj(b1)
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7626073214,.084996112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3450"
  periodicity = 0
  maxiter = 700
  magn = 1.1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3451 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.000275/|pixel2|-real(.025/pixel2)
  b10=conj(pixel2)+abs(.1-pixel2)*cabs(.15-pixel2^2)
  b3=pixel2^8-flip(.2/pixel2)
  b6=|pixel2/2|+asin(pixel2/2)-2*real(pixel2/2)
  b5=b6+b10^4*b3+1.4*b1+.03/pixel2+.29
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748600289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3451"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,.2)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3452 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=|pixel2^4|*pixel2^6
  b5=b2-.001/pixel2-sin(.01/pixel2)+tan(.115/pixel2)-.36
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748600289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3452"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3453 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.000275/|pixel2|-real(0.025/pixel2)
  b5=|pixel2^7-b1|*pixel2^4-|pixel2^4+b1|-flip(.12/pixel2)+b1+.26
  IF (@p3==1)
    z = b5^.845
  ELSEIF (@p3==0)
    z = -b5
  ELSE
    IF (whitesq)
      z = b5^.845
    ELSE
      z = -b5
    ENDIF
  ENDIF
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748600289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3453"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
}

Carr3454 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=conj(pixel2/3)-sin(pixel2/2)-.06/pixel2
  b3=conj(pixel2^2)*abs(pixel2^3)-conj(.08/pixel2)-.125/pixel2
  z=|pixel2|*pixel2*b3-b2-.17
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3454"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3455 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=conj(pixel2/3)-sin(pixel2/2)-.06/pixel2
  b3=conj(pixel2^2)*abs(pixel2^3)-conj(.08/pixel2)-.125/pixel2
  z=|pixel2|*pixel2*b3-b2-.1/pixel2+.066
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3455"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,.05)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3456 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=cabs(pixel2^2)-cos(.066/pixel2)
  b2=pixel2*1.25
  z=flip(conj(pixel2^2)*conj(b3/2))/conj(.3/pixel2)*b2 \
    -.1/pixel2+.066
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3456"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3457 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), op = pixel2/3
  b2=conj(op)*sin(pixel2/2)-.06/pixel2
  b3=conj(op)*abs(op)*conj(.08/pixel2)-.125/pixel2
  z=|pixel2|*b3+b2-.1/pixel2+.066
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3457"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,.3)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3458 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=|pixel2/2|*flip(pixel2/1.5)-.06/pixel2
  b3=cabs(pixel2^2)*(conj(pixel2/3)*abs(pixel2/3))\
     *conj(.08/pixel2)+.125/pixel2
  z=2*conj(pixel2^2)*pixel2*b3^2+b2-.1/pixel2-conj(.1/pixel2)+.066
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3458"
  periodicity = 0
  maxiter = 700
  magn = .9
  center = (0,-.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3459 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=pixel2^4-(.1/pixel2)^4+.15
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3459"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3460 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=pixel2^4-(.03/pixel2)^4-.15
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3460"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3461 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=|pixel2^4|-flip(abs(.03/pixel2))^4-.106/pixel2 \
    -flip(.02/pixel2)-.035
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3461"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3462 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=pixel2/2-1/|cos(asin(3*pixel2+.52))|+flip(.1/pixel2)
  z=b3*flip(|pixel2^4|)-flip(abs(.03/pixel2))^4-.106/pixel2 \
    -flip(.02/pixel2)-.035
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3462"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3463 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=flip(abs(pixel2))^4/flip(asinh(pixel2))
  b2=|pixel2^2|-flip(abs(.03/pixel2))^4-.106/pixel2 \
     -flip(.02/pixel2)
  z=.5*pixel2+b3*|b2|-.1/pixel2+.066
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3463"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3464 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=pixel2^2+conj(.01/pixel2+sin(.01/pixel2))
  z=b3*pixel2^4-(.03/pixel2)^4+.0665
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3464"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3465 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=tanh(pixel2^2)-((.3,.6)+pixel2)*pixel2/.2
  z=|b3|*pixel2^4-(.02/pixel2)^4+.0667
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3465"
  periodicity = 0
  maxiter = 700
  magn = 1.8
  center = (0,-.2)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3466 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=conj(pixel2^7)*cotan(|pixel2|)^1.5-tanh(|pixel2/5|)
  z=b3*|pixel2^4|-(.024/pixel2)^4+.066818
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3466"
  periodicity = 0
  maxiter = 700
  magn = 1.1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3467 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=|pixel2^2|*conj(pixel2^5)-(sin(.026/pixel2))^4+.066818
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(.378083004695,.227300793319)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3467"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3468 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=|pixel2^2|*conj(pixel2^5)-(sin(.026/pixel2))^4+.066818
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3468"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3469 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=conj(pixel2^5)-(.026/pixel2)^4-.001/pixel2+.066825
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3469"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3470 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=pixel2^2*|pixel2^3|
  z=b3-(.026/pixel2)^4-.001/pixel2+.066825
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3470"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3471 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.000051/|pixel2|-real(.025/pixel2)
  b3=pixel2^3*(|pixel2^3|-b1)*(pixel2+b1)
  z=pixel2/2*b3^0.75-(.026/pixel2)^4-.0001/pixel2+.066824
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3471"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3472 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^4)*flip(cabs(pixel2^2))
  b3=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  z=b3^2.75*conj(pixel2^4)-flip(.024/pixel2)^4-.158
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3472"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3473 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^4)*flip(cabs(pixel2^2))
  b3=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  z=flip(b3^2.75)*conj(pixel2^4)-flip(.026/pixel2)^3.3-.15823
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3473"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3474 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b3=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  z=flip(b3^2.75)*flip(pixel2^4)-flip(.026/pixel2)^3.3-.15823
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3474"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3475 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b3=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  z=flip(b3^2.75)*conj(pixel2^4)-flip(0.026/pixel2)^3.3-.15823
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3475"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3476 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b3=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  z=flip(b3^2.35)*cabs(pixel2^2)*pixel2^2-flip(.028/pixel2)^3.3 \
    +.15823
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3476"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3477 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b3=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  z=|b3^2.35|*pixel2^4-asin(flip(.033/pixel2))^3.3+.15825
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3477"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3478 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b3=b2^2.75-conj(.035/pixel2-flip(.01/pixel2))
  z=|b3^2.35|*|pixel2^2|*pixel2^2-asin(flip(.031/pixel2))^2.9 \
    +.15841
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7456,-.134)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3478"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3479 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=|pixel2|*pixel2^2
  z=b3^3.35-flip(0.03/pixel2)^3.3+.15824
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3479"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3480 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=flip(conj(.1+pixel2))*pixel2-conj(.1/pixel2) \
     +sqr(flip(.1/pixel2))
  b3a=2*|pixel2|*pixel2^2
  z=|b3a|*b3^3.05-flip(.033/pixel2)^3.15+.15841
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748800289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3480"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3481 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b3=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  z=flip(b3^2.35)*cabs(pixel2^2)*pixel2^2-flip(.028/pixel2)^3.3 \
    +.15831
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748795289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3481"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3482 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.001491/|pixel2|-real(.025/pixel2)
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b3=(b2^2+b1^4)-conj(.035/pixel2-flip(.01/pixel2))
  z=flip(b3^2.35)*cabs(pixel2^2)*pixel2^2-flip(.028/pixel2)^3.3 \
    +.15831
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791289672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3482"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3483 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=conj(abs(pixel2^2))-(.066/pixel2)^3
  b2=conj(pixel2-.01/pixel2)*flip(pixel2+.1/pixel2)
  b3a=flip(conj(pixel2^8)*b3)/conj(.3/pixel2)*b2/2
  z=pixel2*flip(b3a^3.75)-flip(.027/pixel2)^3.6+.15827
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3483"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3484 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=conj(flip(pixel2^2))-flip(.066/pixel2)^3
  b2=conj(pixel2-.01/pixel2)*flip(pixel2+.1/pixel2)
  b3a=flip(pixel2^8)*b3/conj(0.3/pixel2)*b2/2
  z=conj(pixel2^4)*flip(b3a^3.75) \
    -flip(.023/pixel2-|.0095/pixel2|)^3.6+.15827
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3484"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3485 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=pixel2^4*|pixel2^2|
  b3=b2-.01/pixel2-conj(.1/pixel2)
  z=|pixel2^3|*flip(b3^3.75) \
    -flip(.024/pixel2-|.0095/pixel2|)^3.395+.15823
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3485"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3486 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.001491/|pixel2|-real(.025/pixel2)
  b3=pixel2^5-|pixel2^1.5|
  z=|pixel2^3-b1^3|*flip(b3^3.75) \
    -flip(.021/pixel2-|.0095/pixel2|)^3.415+.15827
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3486"
  periodicity = 0
  maxiter = 700
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3487 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=.001491/|pixel2|-real(.025/pixel2)
  b3=pixel2^5-flip(|pixel2^1.5|)
  z=|pixel2^2-b1^3|*flip(b3^2.95) \
    -flip(.038/pixel2-|.0095/pixel2|)^3.615+.15827
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3487"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,.12)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3488 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3=conj(pixel2^5)*abs(pixel2)*pixel2-conj(.066/pixel2) \
     -flip(.00510/pixel2)^2
  z=|pixel2^3|*flip(conj(b3^1.95)) \
    -flip(.028/pixel2+|.025/pixel2|)^3.915+.15827
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3488"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3489 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b6=|pixel2+.025/pixel2|^4
  b3a=pixel2/2*b6/cotanh(-0.05/pixel2)-conj(.06/pixel2)\
      -flip(.011/pixel2)
  b3=b3a/tanh(.3/pixel2)
  z=|pixel2^3|*flip(conj(b3^2.05)) \
    -flip(.02/pixel2+|.029/pixel2|)^3.915+.15827
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3489"
  periodicity = 0
  maxiter = 700
  magn = 1.15
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3490 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=cosh(cos(pixel2)), b2=conj(cos(cosh(pixel2)))
  b6=(cabs(pixel2)*conj(flip(pixel2))+0.22)/b2
  b3=pixel2^6-conj(.006/b1-flip(.04/b2-conj(.01/b6)))
  z=|pixel2^3|*flip(conj(b3^2.01)) \
    -flip(.026/pixel2+|.029/pixel2|)^3.915+.15827
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3490"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3491 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2)*conj(flip(pixel2^3))
  b3=b2*abs(pixel2^7)+flip(.01/pixel2)+conj(.01/pixel2)
  z=flip(b3^2.35)*cabs(pixel2^2)*pixel2^2 \
    -flip(0.042/pixel2)^3.3+.15831
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3491"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3492 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3a=|.0175/pixel2|
  b2=abs(pixel2^2)*conj(flip(pixel2^3))
  b3=|b2^3|/pixel2^3*cabs(pixel2^4)-flip(.01/pixel2) \
     -conj(.01/pixel2)-conj(b3a)
  z=flip(b3^2.35)*cabs(pixel2^2)*pixel2^2-flip(0.042/pixel2)^3.3\
    +0.15831
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3492"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3493 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=pixel2-conj(.1/pixel2-flip(.01/pixel2+cabs(.01/pixel2)))
  b3=|pixel2^6-conj(.1/b1-flip(.01/b1+cabs(.01/b1)))|
  z=flip(b3^3.6)*cabs(pixel2^2)*pixel2^2 \
    -flip(0.052/pixel2)^2.9+.15831
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3493"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3494 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b6=conj(.015/pixel2), b7=flip(.01/pixel2)
  b8=cabs(.002/pixel2+cabs(.002/pixel2))
  b2=(b8-b6-b7)/.55-.001/pixel2
  b2a=conj(pixel2^4/tanh(.3/pixel2))+.1/pixel2
  b3=conj(cabs(b2a^6)-b2)-conj(.1/pixel2-flip(.01/pixel2))
  b3a=|pixel2-b7^2|/cos(|.6/pixel2|)
  z=b3a*conj(b3^3.4)*flip(conj(pixel2^2.35))*pixel2^2\
    -flip(.032/pixel2)^2.9+.15831
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748791789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3494"
  periodicity = 0
  maxiter = 700
  magn = 1.8
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3495 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b3a=cabs(pixel2^5)-cos(.066/pixel2)^3
  b2=conj(pixel2-.01/pixel2)*flip(pixel2+.1/pixel2)
  b3=flip(|pixel2|^6*b3a)/conj(.3/pixel2)*b2/2
  z=conj(b3^3.4)*flip(conj(pixel2^2.35))*pixel2^2\
    -flip(.032/pixel2)^2.9+.15836
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748831789672,-.1349866189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3495"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3496 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b1=abs(.11/pixel2), b2=|pixel2^4|, b3=b2-conj(0.09/pixel2)
  b3=|pixel2|*pixel2*b3*|b2/2|-flip(.01/pixel2)+b1
  b3a=conj(b3^3.5)*abs(.5*b3)
  z=b3a^1.5*flip(conj(pixel2^2.47))*pixel2^2\
    -flip(.0335/pixel2)^2.9+.15836
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748831789672,-.1349866189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3496"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3497 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b2a=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  b3=flip(b2a^2.35)*cabs(pixel2^2)*pixel2^2 \
     -flip(.028/pixel2)^3.3
  b3a=|b3^3.5|/9*abs(b3)
  z=b3a^2.5*flip(conj(pixel2^3.47))*pixel2^2\
    -flip(.0435/pixel2)^2.85+.15836
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = 0
  ENDIF
  z=z^2+(-0.748800289672,-0.1350036189)-0.0000035
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3497"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3498 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b2a=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  b3=flip(b2a^2.35)*cabs(pixel2^2)*pixel2^2 \
     -flip(.028/pixel2)^3.3
  b3a=conj(b3^3.5)*abs(b3)+flip(.029/pixel2)
  z=b3a^2.5*flip(conj(pixel2^3.47))*pixel2^2\
    -flip(.0445/pixel2)^2.85+.15836
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748803789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3498"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}

Carr3499 (YAXIS) {; Updated for UF2 by Erik Reckase, February 2000
                  ; Modified Sylvie Gallet frm. 
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(cabs(pixel2^2))
  b2a=b2^2-conj(.035/pixel2-flip(.01/pixel2))
  b3=flip(b2a^2.35)*cabs(pixel2^2)*pixel2^2 \
     -flip(.028/pixel2)^3.3
  b3a=conj(b3^3.5)*abs(b3)+conj(flip(.021/pixel2)) \
      +sinh(.0125/pixel2)
  z=b3a^2.5*flip(conj(pixel2^3.47))/conj(pixel2^2.5) \
     -flip(0.0445/pixel2)^2.99+.15836
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.748803789672,-.1350036189)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "Carr 3499"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
}
