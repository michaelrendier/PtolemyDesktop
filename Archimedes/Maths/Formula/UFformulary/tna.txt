mothstyx2{
;  by Ted Nason 2004  archosaur@comcast.net
init:
pixel2=-abs(real(pixel))+flip(imag(pixel))
m4=conj(.1/pixel)


  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + real(pixel2)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + imag(pixel2)
  z = xx + flip(yy)


  ;z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.6
  c=tanh(sinh(1.5/(.224+.612*real(pixel2)+flip(abs(pixel2)))-m4))


  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  k2 = .15/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  ;z = .2*real(z^2)*z + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "mothstyx"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
  endparam
  param start
    caption = "Starting point"
    default = (0,0)
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "mothstyx-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


mothstyx{
;  by Ted Nason 2004  archosaur@comcast.net
init:
pixel2=-abs(real(pixel))+flip(imag(pixel))
m4=conj(.1/pixel)


  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + real(#pixel)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + imag(#pixel)
  z = xx + flip(yy)


  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.6
  c=tanh(sinh(1.5/(.224+.612*real(pixel2)+flip(abs(pixel2)))-m4))


  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  k2 = .15/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "mothstyx"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
  endparam
  param start
    caption = "Starting point"
    default = (0,0)
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "mothstyx-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


carr2082tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init: 
  c=pixel^2/conj(pixel)
  z=-flip(imag(pixel))*|pixel|-conj(.1/pixel)-flip(.01/pixel)
  z1 = 1.5*z, z2 = 2.25*z, z3 = 3.375*z, z4 = 5.0625*z
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=c=z1
  ELSEIF (iter==l2)
    z=c=z2
  ELSEIF (iter==l3)
    z=c=z3
  ELSEIF (iter==l4)
    z=c=z4
  ENDIF
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2082tna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (-.5,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2082tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2082tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init: 
  c=@seed^2/conj(@seed)
  z=-flip(imag(pixel))*|pixel|-conj(.1/pixel)-flip(.01/pixel)
  z1 = 1.5*z, z2 = 2.25*z, z3 = 3.375*z, z4 = 5.0625*z
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=c=z1
  ELSEIF (iter==l2)
    z=c=z2
  ELSEIF (iter==l3)
    z=c=z3
  ELSEIF (iter==l4)
    z=c=z4
  ENDIF
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2082tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (-.5,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2082tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2081tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=pixel
  z=-flip(imag(pixel))*|pixel|-conj(.1/pixel)-flip(.01/pixel)
  z1 = 1.5*z, z2 = 2.25*z, z3 = 3.375*z, z4 = 5.0625*z
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=c=z1
  ELSEIF (iter==l2)
    z=c=z2
  ELSEIF (iter==l3)
    z=c=z3
  ELSEIF (iter==l4)
    z=c=z4
  ENDIF
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2081tna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (-.5,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2081tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2081tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=@seed
  z=-flip(imag(pixel))*|pixel|-conj(.1/pixel)-flip(.01/pixel)
  z1 = 1.5*z, z2 = 2.25*z, z3 = 3.375*z, z4 = 5.0625*z
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=c=z1
  ELSEIF (iter==l2)
    z=c=z2
  ELSEIF (iter==l3)
    z=c=z3
  ELSEIF (iter==l4)
    z=c=z4
  ENDIF
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2081tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (-.5,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2081tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2086tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init: 
  c=conj(pixel-.33)
  z=c*pixel^2-conj(.1/pixel)+flip(.01/pixel)^2
  z1 = 2.25*z, z2 = 3.25*z, z3 = 5.375*z, z4 = 10.0625*z
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0, d=.0125/conj(z)
loop:
  IF (iter==l1)
    z=c=z1
  ELSEIF (iter==l2)
    z=c=z2
  ELSEIF (iter==l3)
    z=c=z3
  ELSEIF (iter==l4)
    z=c=z4
  ENDIF
  z = (z-d)^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2086tna"
  periodicity = 0
  maxiter = 500
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2086tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2086tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init: 
  c=conj(@seed-.33)
  z=c*pixel^2-conj(.1/pixel)+flip(.01/pixel)^2
  z1 = 2.25*z, z2 = 3.25*z, z3 = 5.375*z, z4 = 10.0625*z
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0, d=.0125/conj(z)
loop:
  IF (iter==l1)
    z=c=z1
  ELSEIF (iter==l2)
    z=c=z2
  ELSEIF (iter==l3)
    z=c=z3
  ELSEIF (iter==l4)
    z=c=z4
  ENDIF
  z = (z-d)^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2086tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2086tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3133tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=pixel2^4-conj(.1/pixel2)-flip(.1/pixel2)^3+.3
  c=z*flip(z)
  c1=1.5*cabs(z/2),c2=2.25*conj(z/2)
  c3=3.375*flip(z/2),c4=5.0625*z^2
  k = (-.7456,-.132)
  ct = (c/8) + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    ct = (c1/8) + k, z = 0
  ELSEIF (iter==l2)
    ct = (c2/8) + k, z = 0
  ELSEIF (iter==l3)
    ct = (c3/8) + k, z = 0
  ELSEIF (iter==l4)
    ct = (c4/8) + k, z = 0
  ENDIF
  z = z^2 + ct
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "carr3133tna"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3133tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3133tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=pixel2^4-conj(.1/pixel2)-flip(.1/pixel2)^3+.3
  zz=pixel3^4-conj(.1/pixel3)-flip(.1/pixel3)^3+.3
  c=zz*flip(zz)
  c1=1.5*cabs(z/2),c2=2.25*conj(z/2)
  c3=3.375*flip(z/2),c4=5.0625*z^2
  k = (-.7456,-.132)
  ct = (c/8) + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    ct = (c1/8) + k, z = 0
  ELSEIF (iter==l2)
    ct = (c2/8) + k, z = 0
  ELSEIF (iter==l3)
    ct = (c3/8) + k, z = 0
  ELSEIF (iter==l4)
    ct = (c4/8) + k, z = 0
  ENDIF
  z = z^2 + ct
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "carr3133tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1.4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3133tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2530tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  c=.1/pixel2/flip(.88/pixel2^2), z=c-.3
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0010995
  k2 = .79/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2530tna"
  periodicity = 0
  maxiter = 500
  magn = .09
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2530tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2530tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  c=.1/pixel3/flip(.88/pixel3^2)
  cc=.1/pixel2/flip(.88/pixel2^2)
  z=cc-.3
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) + .0010995
  k2 = .79/pixel3
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2530tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .09
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2530tna"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2752tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)-flip(abs(imag(pixel)))
  z=pixel2^4/conj(pixel2^1.5)+.3
  c=.8*pixel2-1/log(pixel2-.1/pixel2+imag(.001/pixel2))
  c = -c, z = -z
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2)
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2752tna"
  periodicity = 0
  maxiter = 500
  magn = 1.7
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2752tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2752tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)-flip(abs(imag(pixel)))
  pixel3=real(@seed)-flip(abs(imag(@seed)))
  z=pixel2^4/conj(pixel2^1.5)+.3
  c=.8*pixel3-1/log(pixel3-.1/pixel3+imag(.001/pixel3))
  c = -c, z = -z
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2)
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2752tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.7
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2752tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2752Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)-flip(abs(imag(pixel)))
  z=pixel2^4/conj(pixel2^1.5)+.3
  c=.8*pixel2-1/log(pixel2-.1/pixel2+imag(.001/pixel2))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2)
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2752Btna"
  periodicity = 0
  maxiter = 500
  magn = 1.7
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2752Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2752Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)-flip(abs(imag(pixel)))
  pixel3=real(@seed)-flip(abs(imag(@seed)))
  z=pixel2^4/conj(pixel2^1.5)+.3
  c=.8*pixel3-1/log(pixel3-.1/pixel3+imag(.001/pixel3))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2)
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2752Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.7
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2752Btna"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3036tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=cabs(.33-cos(pixel2))/(.33-tan(3*pixel2))-.4
  c=conj(pixel2)*(pixel2-flip(.001/pixel2)) \
    *(pixel2-conj(.01/pixel2))*(pixel2-conj(.1/z))+.5
  c = -conj(c), z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.6856,-.132) + .01
  ct = c/6 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    ct = c1/6 + k, z = 0
  ELSEIF (iter==l2)
    ct = c2/6 + k, z = 0
  ELSEIF (iter==l3)
    ct = c3/6 + k, z = 0
  ELSEIF (iter==l4)
    ct = c4/6 + k, z = 0
  ENDIF
  z = z^2 + ct
  iter=iter+1
bailout:
  real(z)<=bailout
default:
  title = "carr3036tna"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3036tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3036tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=cabs(.33-cos(pixel2))/(.33-tan(3*pixel2))-.4
  c=conj(pixel3)*(pixel3-flip(.001/pixel3)) \
    *(pixel3-conj(.01/pixel3))*(pixel3-conj(.1/z))+.5
  c = -conj(c), z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.6856,-.132) + .01
  ct = c/6 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    ct = c1/6 + k, z = 0
  ELSEIF (iter==l2)
    ct = c2/6 + k, z = 0
  ELSEIF (iter==l3)
    ct = c3/6 + k, z = 0
  ELSEIF (iter==l4)
    ct = c4/6 + k, z = 0
  ENDIF
  z = z^2 + ct
  iter=iter+1
bailout:
  real(z)<=bailout
default:
  title = "carr3036tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3036tna"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3007Btna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=(-.7456,-.132)
  z=abs(2*pixel^12)-flip(.1/pixel)-conj(.1/pixel)+.35
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3007Btna"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3007Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3007Btna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=@seed
  z=abs(2*pixel^12)-flip(.1/pixel)-conj(.1/pixel)+.35
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3007Btna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3007Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3018tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel/3.67-conj(flip(pixel/3)), c=(-.7456,-.132)+pixel
  c = -c, z = -z
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = 1.25*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3018tna"
  periodicity = 0
  maxiter = 700
  magn = .9
  center = (0,.4)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3018tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3018tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel/3.67-conj(flip(pixel/3))
  c=(-.7456,-.132)+@seed
  c = -c, z = -z
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = 1.25*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3018tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .9
  center = (0,.4)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3018tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3018Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel/3.67-conj(flip(pixel/3)), c=(-.7456,-.132)+pixel
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = 1.25*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3018Btna"
  periodicity = 0
  maxiter = 700
  magn = .9
  center = (0,.4)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3018Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3018Btna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel/3.67-conj(flip(pixel/3))
  c=(-.7456,-.132)+@seed
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = 1.25*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3018Btna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .9
  center = (0,.4)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3018Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3005tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b4=conj(pixel2^3)-conj(.1/pixel2-flip(.01/pixel2))+.4
  z=flip(conj(1.25*pixel2))-flip(.01/pixel2) \
    -conj(.001/pixel2)+.4
  c = -sqr(b4), z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/10 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/10 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/10 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/10 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/10 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3005tna"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3005tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3005tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  b5=conj(pixel3^3)-conj(.1/pixel3-flip(.01/pixel3))+.4
  z=flip(conj(1.25*pixel2))-flip(.01/pixel2) \
    -conj(.001/pixel2)+.4
  c = -sqr(b5), z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/10 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/10 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/10 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/10 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/10 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3005tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3005tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3005Btna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b4=conj(pixel2^3)-conj(.1/pixel2-flip(.01/pixel2))+.4
  z=flip(conj(1.25*pixel2))-flip(.01/pixel2) \
    -conj(.001/pixel2)+.4
  z=flip(conj(1.25*pixel2))-flip(.01/pixel2) \
    -conj(.001/pixel2)+.4
  c = conj(b5)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/10 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/10 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/10 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/10 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/10 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3005Btna"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3005Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3005Btna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  b5=conj(pixel3^3)-conj(.1/pixel3-flip(.01/pixel3))+.4
  z=flip(conj(1.25*pixel2))-flip(.01/pixel2) \
    -conj(.001/pixel2)+.4
  c = conj(b5)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/10 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/10 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/10 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/10 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/10 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3005Btna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1.2
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3005Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3020tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel-.2-.01/pixel-cos(.01/pixel)+conj(.1/pixel)
  c=conj(pixel)-flip(asin(.01/pixel))+conj(asin(.1/pixel))
  c = -c, z = -z
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3020tna"
  periodicity = 0
  maxiter = 700
  magn = .4
  center = (.8,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3020tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3020tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel-.2-.01/pixel-cos(.01/pixel)+conj(.1/pixel)
  c=conj(@seed)-flip(asin(.01/@seed))+conj(asin(.1/@seed))
  c = -c, z = -z
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3020tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .4
  center = (.8,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3020tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3020Btna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel-.2-.01/pixel-cos(.01/pixel)+conj(.1/pixel)
  c=conj(pixel)-flip(asin(.01/pixel))+conj(asin(.1/pixel))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3020Btna"
  periodicity = 0
  maxiter = 700
  magn = .4
  center = (.8,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3020Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3020Btna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=pixel-.2-.01/pixel-cos(.01/pixel)+conj(.1/pixel)
  c=conj(@seed)-flip(asin(.01/@seed))+conj(asin(.1/@seed))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3020Btna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .4
  center = (.8,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3020Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3403tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=|pixel2|*pixel2^2*abs(pixel2)^4-conj(.15/pixel2) \
    -flip(.015/pixel2)-.06/pixel2+.23
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.084496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "carr3403tna"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3403tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3403tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=|pixel2|*pixel2^2*abs(pixel3)^4-conj(.15/pixel2) \
    -flip(.015/pixel3)-.06/pixel3+.23
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0, float zro=0
loop:
  IF ((iter==l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+(-.7612073214,.084496112)
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "carr3403tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3403tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}



carr3008tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=pixel-conj(.01/pixel-flip(.001/pixel))
  z=1-abs(imag(c)-real(c))+flip(1-abs(1-real(c)-imag(c)))
  c = -c, z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3008tna"
  periodicity = 0
  maxiter = 700
  magn = .8
  center = (.3,.3)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3008tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3008tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  cc=@seed-conj(.01/@seed-flip(.001/@seed))
  c=pixel-conj(.01/pixel-flip(.001/pixel))
  z=1-abs(imag(c)-real(c))+flip(1-abs(1-real(c)-imag(c)))
  c = -cc, z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3008tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .8
  center = (.3,.3)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3008tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3008Btna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=pixel-conj(.01/pixel-flip(.001/pixel))
  z=1-abs(imag(c)-real(c))+flip(1-abs(1-real(c)-imag(c)))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3008Btna"
  periodicity = 0
  maxiter = 700
  magn = .8
  center = (.3,.3)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3008Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3008Btna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  cc=pixel-conj(.01/pixel-flip(.001/pixel))
  c=@seed-conj(.01/@seed-flip(.001/@seed))
  z=1-abs(imag(cc)-real(cc))+flip(1-abs(1-real(cc)-imag(cc)))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3008Btna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .8
  center = (.3,.3)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3008Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3019tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=flip(.001/pixel)-pixel, z=conj(.001/pixel)-pixel
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3019tna"
  periodicity = 0
  maxiter = 700
  magn = .3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3019tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3019tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=flip(.001/@seed)-@seed, z=conj(.001/pixel)-pixel
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3019tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3019tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3019Btna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=pixel-flip(.001/pixel), z=pixel-conj(.001/pixel)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3019Btna"
  periodicity = 0
  maxiter = 700
  magn = .3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3019Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3019Btna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=@seed-flip(.001/@seed), z=pixel-conj(.001/pixel)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7456,-.132)
  ct = conj(c/4) + k
  zm = 25/56
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = conj(c/4) + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = conj(c/4) + k, z = 0
  ENDIF
  z = zm*c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3019Btna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3019Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3010tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b4=pixel2-conj(.01/pixel2-flip(.001/pixel2))
  z=1-abs(imag(b4)-real(b4/2)) \
     +flip(1-abs(1-real(b4)-flip(imag(b4))))
  c = -(b4), z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3010tna"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3010tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3010tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  b4=pixel2-conj(.01/pixel2-flip(.001/pixel2))
  b5=pixel3-conj(.01/pixel3-flip(.001/pixel3))
  z=1-abs(imag(b4)-real(b4/2)) \
     +flip(1-abs(1-real(b4)-flip(imag(b4))))
  c = -(b5), z = -sqr(z)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3010tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3010tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3010Btna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b4=pixel2-conj(.01/pixel2-flip(.001/pixel2))
  z=1-abs(imag(b4)-real(b4/2)) \
     +flip(1-abs(1-real(b4)-flip(imag(b4))))
  c = conj(b4)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3010Btna"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3010Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3010Btna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  b4=pixel2-conj(.01/pixel2-flip(.001/pixel2))
  b5=pixel3-conj(.01/pixel3-flip(.001/pixel3))
  z=1-abs(imag(b4)-real(b4/2)) \
     +flip(1-abs(1-real(b4)-flip(imag(b4))))
  c = conj(b5)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.132,-.6956)
  ct = c/8 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/8 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/8 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/8 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/8 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel2)<=bailout
default:
  title = "carr3010Btna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1
  center = (0,.1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3010Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr1591tna{
; A "Modified Carr" formula, based on the original with permission.
; Functionality added by Sharon S. Schultz, May, 2003.
; Updated for UF2 by Erik Reckase, Jan 2000
; Spider type.
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z = (1/acos(#pixel+#pixel))/@scale
  c = 1/asin(#pixel)
loop:
  z = fn1(fn2(fn3(z*z+c)))
  c = z+c/2.125
bailout:
  |real(z)| <= @bailout
default:
  title = "carr1591tna"
  method = multipass
  periodicity = 0
  maxiter = 100
  magn = .5
  center = (0,0)
heading 
  caption = "Parameters"
endheading
param scale
  caption = "Scale"
  default = 1.0
  hint = "Select a value to scale the image elements."
endparam
param bailout
  caption = "Bailout"
  default = 4
  hint = "Select a bailout value for the formula."
endparam
heading
  caption = "Functions"
endheading
func fn1
  caption = "Change"
  default = ident()
  hint = "Select a function to change the formula."
endfunc
func fn2
  caption = "Change More"
  default = ident()
  hint = "Select a function to change the formula more."
endfunc
func fn3
  caption = "Total Change"
  default = ident()
  hint = "Select a function to totally change the formula."
endfunc
switch:
  type = "carr1591tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr1591tna-Julia{
; A "Modified Carr" formula, based on the original with permission.
; Functionality added by Sharon S. Schultz, May, 2003.
; Updated for UF2 by Erik Reckase, Jan 2000
; Spider type.
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z = (1/acos(#pixel+#pixel))/@scale
  c = 1/asin(@seed)
loop:
  z = fn1(fn2(fn3(z*z+c)))
  c = z+c/2.125
bailout:
  |real(z)| <= @bailout
default:
  title = "carr1591tna-Julia"
  method = multipass
  periodicity = 0
  maxiter = 100
  magn = .5
  center = (0,0)
heading 
  caption = "Parameters"
endheading
param scale
  caption = "Scale"
  default = 1.0
  hint = "Select a value to scale the image elements."
endparam
param bailout
  caption = "Bailout"
  default = 4
  hint = "Select a bailout value for the formula."
endparam
heading
  caption = "Functions"
endheading
func fn1
  caption = "Change"
  default = ident()
  hint = "Select a function to change the formula."
endfunc
func fn2
  caption = "Change More"
  default = ident()
  hint = "Select a function to change the formula more."
endfunc
func fn3
  caption = "Total Change"
  default = ident()
  hint = "Select a function to totally change the formula."
endfunc
switch:
  type = "carr1591tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3004tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  b5=conj(pixel)-conj(.1/pixel-flip(.0019/pixel))+.2
  b4=conj(pixel)-flip(.01/pixel)-conj(.001/pixel)
  c = -sqr(b4), z = -sqr(b5)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/10 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/10 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/10 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/10 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/10 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3004tna"
  periodicity = 0
  maxiter = 700
  magn = 1.1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3004tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3004tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. [101324,3444],1996
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  b5=conj(pixel)-conj(.1/pixel-flip(.0019/pixel))+.2
  b4=conj(@seed)-flip(.01/@seed)-conj(.001/@seed)
  c = -sqr(b4), z = -sqr(b5)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = (-.7956,-.132)
  ct = c/10 + k
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, ct = c/10 + k, z = 0
  ELSEIF (iter==l2)
    c = c2, ct = c/10 + k, z = 0
  ELSEIF (iter==l3)
    c = c3, ct = c/10 + k, z = 0
  ELSEIF (iter==l4)
    c = c4, ct = c/10 + k, z = 0
  ENDIF
  z = c*z^2/conj(z+c) + ct
  iter=iter+1
bailout:
  real(z-pixel)<=bailout
default:
  title = "carr3004tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = 1.1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3004tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3509tna{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm.
; Black areas do not exist on Fractint - unable to remove them.
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(abs(pixel2^2))
  b2a=b2^2-.035/pixel2+flip(.01/pixel2)+.004/pixel2
  b3=b2a+pixel2^4*|pixel2^3|/2+.0003/pixel2
  z=asin(b3^2.95)/flip(b3^1.30)-flip(0.039/pixel2)^3.5 \
    -.004/pixel2+.066124
  zmod = (-.748823289672,-.1350036189)
  l1 = round(real(p1)),l2 = round(imag(p1))
  l3 = round(real(p2)),l4 = round(imag(p2))
  float bailout=16,int iter=0, float zro = 0
loop:
  IF ((iter == l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+zmod
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "carr3509tna"
  periodicity = 0
  maxiter = 700
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3509tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr3509tna-Julia{
; Updated for UF2 by Erik Reckase, February 2000
; Modified Sylvie Gallet frm.
; Black areas do not exist on Fractint - unable to remove them.
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  b2=abs(pixel2^2.55)*flip(abs(pixel2^2))
  b2a=b2^2-.035/pixel2+flip(.01/pixel2)+.004/pixel2
  b3=b2a+pixel3^4*|pixel3^3|/2+.0003/pixel3
  z=asin(b3^2.95)/flip(b3^1.30)-flip(0.039/pixel3)^3.5-.004/pixel3+.066124
  zmod = (-.748823289672,-.1350036189)
  l1 = round(real(p1)),l2 = round(imag(p1))
  l3 = round(real(p2)),l4 = round(imag(p2))
  float bailout=16,int iter=0, float zro = 0
loop:
  IF ((iter == l1) || (iter==l2) || (iter==l3) || (iter==l4))
    z = z*zro
  ENDIF
  z=z^2+zmod
  iter=iter+1
bailout:
  |z|<=bailout
default:
  title = "carr3509tna-Julia"
  periodicity = 0
  maxiter = 700
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (200,300)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (400,550)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr3509tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2549tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = 1/pixel2, z=1/(pixel2-exp(op)-tan(|op|)-conj(.1/pixel2))
  c=1/(pixel2-asinh(op-cos(|z|)-|z|)-conj(1/exp(pixel2-.85)))
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel2/imag(p2) + .0002995
  k2 = .09/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.099/c, c=c1
  ELSEIF (iter==l2)
    z=-.099/c, c=c2
  ELSEIF (iter==l3)
    z=-.099/c, c=c3
  ELSEIF (iter==l4)
    z=-.099/c, c=c4
  ELSE
    z=z-.099/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2549tna"
  periodicity = 0
  maxiter = 500
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2549tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2549tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = 1/pixel3, z=1/(pixel2-exp(op)-tan(|op|)-conj(.1/pixel2))
  c=1/(pixel3-asinh(op-cos(|z|)-|z|)-conj(1/exp(pixel3-.85)))
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel3/imag(p2) + .0002995
  k2 = .09/pixel3
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.099/c, c=c1
  ELSEIF (iter==l2)
    z=-.099/c, c=c2
  ELSEIF (iter==l3)
    z=-.099/c, c=c3
  ELSEIF (iter==l4)
    z=-.099/c, c=c4
  ELSE
    z=z-.099/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2549tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2549tna"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2546Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = 1/pixel2, b1=conj(op)/exp(op)
  z=1/(pixel2-atan(op-conj(b1))+conj(op)+flip(op))
  c=1/(pixel2-cos(op-conj(b1))+conj(op)+flip(op))/b1
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel2/imag(p2) + .0002995
  k2 = .09/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c + k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2546Btna"
  periodicity = 0
  maxiter = 500
  magn = .3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2546Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2546Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = 1/pixel3, b1=conj(op)/exp(op)
  z=1/(pixel2-atan(op-conj(b1))+conj(op)+flip(op))
  c=1/(pixel2-cos(op-conj(b1))+conj(op)+flip(op))/b1
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel3/imag(p2) + .0002995
  k2 = .09/pixel3
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c + k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2546Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2546Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}

carr2531Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = .1/pixel2, b=conj(op), b1=flip(op/10)
  c=.75*pixel2-1/cotan(pixel2)-1/cos(pixel2+.2)-b-b1
  z=(.85*pixel2-sinh(op))/b*b1-.2
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0003995
  k2 = .39/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2531Btna"
  periodicity = 0
  maxiter = 500
  magn = .35
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2531Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2531Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = .1/pixel3, b=conj(op), b1=flip(op/10)
  c=.75*pixel3-1/cotan(pixel3)-1/cos(pixel3+.2)-b-b1
  z=(.85*pixel2-sinh(op))/b*b1-.2
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = @seed/imag(p2) + .0003995
  k2 = .39/@seed
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2531Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .35
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2531Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2736tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=pixel-conj(.1/pixel-sin(.01/pixel))
  z=conj(pixel/2)-sqrt(cos(.1/pixel))*tan(.1/pixel)
  IF (@p3==0)
    c = -c, z = -z
  ELSEIF (@p3==2)
    IF (whitesq)
      c = -c, z = -z
    ENDIF
  ENDIF
  k = pixel/imag(p2) - .0055
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2736tna"
  periodicity = 0
  maxiter = 500
  magn = .9
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
switch:
  type = "carr2736tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
  p3 = p3
}


carr2736tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=@seed-conj(.1/@seed-sin(.01/@seed))
  z=conj(pixel/2)-sqrt(cos(.1/pixel))*tan(.1/pixel)
  IF (@p3==0)
    c = -c, z = -z
  ELSEIF (@p3==2)
    IF (whitesq)
      c = -c, z = -z
    ENDIF
  ENDIF
  k = pixel/imag(p2) - .0055
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2736tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .9
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
switch:
  type = "carr2736tna"
  bailout = bailout
  p1 = p1
  p2 = p2
  p3 = p3
}


carr2736Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=pixel-conj(.1/pixel-sin(.01/pixel))
  z=conj(pixel/2)-sqrt(cos(.1/pixel))*tan(.1/pixel)
  IF (@p3==0)
    c = -c, z = -z
  ELSEIF (@p3==2)
    IF (whitesq)
      c = -c, z = -z
    ENDIF
  ENDIF
  k = pixel/imag(p2) - .0055
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2736Btna"
  periodicity = 0
  maxiter = 500
  magn = .9
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
switch:
  type = "carr2736Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
  p3 = p3
}


carr2736Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  c=@seed-conj(.1/@seed-sin(.01/@seed))
  z=conj(pixel/2)-sqrt(cos(.1/pixel))*tan(.1/pixel)
  IF (@p3==0)
    c = -c, z = -z
  ELSEIF (@p3==2)
    IF (whitesq)
      c = -c, z = -z
    ENDIF
  ENDIF
  k = @seed/imag(p2) - .0055
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2736Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .9
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
switch:
  type = "carr2736Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
  p3 = p3
}


carr2744tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)-flip(abs(imag(pixel)))
  c=flip(imag(pixel2))*log(.1/pixel2-conj(.1/pixel2))+.3, z=pixel2
  IF (@p3==0)
    c = -c, z = -z
  ELSEIF (@p3==2)
    IF (whitesq)
      c = -c, z = -z
    ENDIF
  ENDIF
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2)
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2744tna"
  periodicity = 0
  maxiter = 500
  magn = 1.5
  center = (0,.001)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
switch:
  type = "carr2744tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
  p3 = p3
}

carr2744tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)-flip(abs(imag(pixel)))
  pixel3=real(@seed)-flip(abs(imag(@seed)))
  c=flip(imag(@seed))*log(.1/@seed-conj(.1/@seed))+.3, z=pixel2
  IF (@p3==0)
    c = -c, z = -z
  ELSEIF (@p3==2)
    IF (whitesq)
      c = -c, z = -z
    ENDIF
  ENDIF
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2)
  l1=round(real(p1)),l2=round(imag(p1))
  l3=round(real(p2)),l4=round(imag(p2))
  float bailout=16,int iter=0
loop:
  IF (iter==l1)
    c = c1, z = 0
  ELSEIF (iter==l2)
    c = c2, z = 0
  ELSEIF (iter==l3)
    c = c3, z = 0
  ELSEIF (iter==l4)
    c = c4, z = 0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)|<=bailout
default:
  title = "carr2744tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.5
  center = (0,.001)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p3
    caption = "Layer Option"
    enum = "Option 1" "Option 2" "Original"
    default = 2
    hint = "Options 1 and 2 are the two separate layers of this \
            formula.  The 'Original' option uses the original \
            PHC formula."
  endparam
switch:
  type = "carr2744tna"
  bailout = bailout
  p1 = p1
  p2 = p2
  p3 = p3
}


carr2533tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=tan(.75/pixel2)^1.75-conj(.1/pixel2)
  c=tan(.75/pixel2)^1.65-conj(.1/pixel2)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0010995
  k2 = .24/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2533tna"
  periodicity = 0
  maxiter = 500
  magn = .5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2533tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2533tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=tan(.75/pixel2)^1.75-conj(.1/pixel2)
  c=tan(.75/pixel3)^1.65-conj(.1/pixel3)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0010995
  k2 = .24/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2533tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2533tna"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2533Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=tan(.75/pixel2)^1.75-conj(.1/pixel2)
  c=tan(.75/pixel2)^1.65-conj(.1/pixel2)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0010995
  k2 = .24/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2533Btna"
  periodicity = 0
  maxiter = 500
  magn = .5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2533Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2533Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=tan(.75/pixel2)^1.75-conj(.1/pixel2)
  c=tan(.75/pixel3)^1.65-conj(.1/pixel3)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) + .0010995
  k2 = .24/pixel3
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2533Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2533Btna"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2510tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)+flip(abs(imag(pixel)))
  z=flip(conj(abs(pixel2^3)))-conj(.1/pixel2)
  c=(-.7456,-.132)/1.25
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2510tna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2510tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2510tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)+flip(abs(imag(pixel)))
  pixel3=real(@seed)+flip(abs(imag(@seed)))
  z=flip(conj(abs(pixel3^3)))-conj(.1/pixel3)
  c=(-.7456,-.132)/1.25
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2510tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2510tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2510Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=real(pixel)+flip(abs(imag(pixel)))
  z=flip(conj(abs(pixel2^3)))-conj(.1/pixel2)
  c=(-.7456,-.132)/1.25
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2510Btna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2510Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2510Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=real(pixel)+flip(abs(imag(pixel)))
  pixel3=real(@seed)+flip(abs(imag(@seed)))
  z=flip(conj(abs(pixel2^3)))-conj(.1/pixel2)
  c=(-.7456,-.132)/1.25
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) + .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2510Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2510Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2501tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  c=(-.8006,.116), m4=conj(.005/pixel2)-sqr(flip(.09/pixel2))
  z=flip(1/atan(1-pixel2))*sqr(pixel2)-m4
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) - .0061
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    c = c1, z=0
  ELSEIF (iter==l2)
    c = c2, z=0
  ELSEIF (iter==l3)
    c = c3, z=0
  ELSEIF (iter==l4)
    c = c4, z=0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2501tna"
  periodicity = 0
  maxiter = 500
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2501tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2501tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  c=(-.8006,.116), m4=conj(.005/pixel3)-sqr(flip(.09/pixel3))
  z=flip(1/atan(1-pixel2))*sqr(pixel2)-m4
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) - .0061
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    c = c1, z=0
  ELSEIF (iter==l2)
    c = c2, z=0
  ELSEIF (iter==l3)
    c = c3, z=0
  ELSEIF (iter==l4)
    c = c4, z=0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2501tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2501tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2501Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  c=(-.8006,.116), m4=conj(.005/pixel2)-sqr(flip(.09/pixel2))
  z=flip(1/atan(1-pixel2))*sqr(pixel2)-m4
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) - .0061
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    c = c1, z=0
  ELSEIF (iter==l2)
    c = c2, z=0
  ELSEIF (iter==l3)
    c = c3, z=0
  ELSEIF (iter==l4)
    c = c4, z=0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2501Btna"
  periodicity = 0
  maxiter = 500
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2501Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2501Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  c=(-.8006,.116), m4=conj(.005/pixel3)-sqr(flip(.09/pixel3))
  z=flip(1/atan(1-pixel2))*sqr(pixel2)-m4
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) - .0061
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    c = c1, z=0
  ELSEIF (iter==l2)
    c = c2, z=0
  ELSEIF (iter==l3)
    c = c3, z=0
  ELSEIF (iter==l4)
    c = c4, z=0
  ENDIF
  c = c + k
  z = z*z + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2501Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = 1.5
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2501Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}



carr2525tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=.8/pixel2, c=conj(cos(z^z/pixel2-2))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0016995
  k2 = .2/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2525tna"
  periodicity = 0
  maxiter = 500
  magn = .05
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2525tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2525tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=.8/pixel2, c=conj(cos(z^z/pixel3-2))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0016995
  k2 = .2/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2525tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .05
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2525tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2525Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=.8/pixel2, c=conj(cos(z^z/pixel2-2))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .0016995
  k2 = .2/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2525Btna"
  periodicity = 0
  maxiter = 500
  magn = .05
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2525Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2525Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=.8/pixel2, c=conj(cos(z^z/pixel3-2))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) + .0016995
  k2 = .2/pixel3
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*z^3 + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2525Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .05
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2525Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2548tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = 1/pixel2, z=1/(pixel2-flip(op)-tan(|op|)-conj(op))
  c=1/(pixel2-atan(op-cosh(|z|)-|z|)-conj(1/exp(pixel2-.85)))
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel2/imag(p2) + .0002995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.099/c, c=c1
  ELSEIF (iter==l2)
    z=-.099/c, c=c2
  ELSEIF (iter==l3)
    z=-.099/c, c=c3
  ELSEIF (iter==l4)
    z=-.099/c, c=c4
  ELSE
    z=z-.099/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2548tna"
  periodicity = 0
  maxiter = 500
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2548tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2548tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  op = 1/pixel2, z=1/(pixel2-flip(op)-tan(|op|)-conj(op))
  c=1/(pixel3-atan(op-cosh(|z|)-|z|)-conj(1/exp(pixel3-.85)))
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel2/imag(p2) + .0002995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.099/c, c=c1
  ELSEIF (iter==l2)
    z=-.099/c, c=c2
  ELSEIF (iter==l3)
    z=-.099/c, c=c3
  ELSEIF (iter==l4)
    z=-.099/c, c=c4
  ELSE
    z=z-.099/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2548tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2548tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2548Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  op = 1/pixel2, z=1/(pixel2-flip(op)-tan(|op|)-conj(op))
  c=1/(pixel2-atan(op-cosh(|z|)-|z|)-conj(1/exp(pixel2-.85)))
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel2/imag(p2) + .0002995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.099/c, c=c1
  ELSEIF (iter==l2)
    z=-.099/c, c=c2
  ELSEIF (iter==l3)
    z=-.099/c, c=c3
  ELSEIF (iter==l4)
    z=-.099/c, c=c4
  ELSE
    z=z-.099/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2548Btna"
  periodicity = 0
  maxiter = 500
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2548Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2548Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  op = 1/pixel2, z=1/(pixel2-flip(op)-tan(|op|)-conj(op))
  c=1/(pixel3-atan(op-cosh(|z|)-|z|)-conj(1/exp(pixel3-.85)))
  c1=0.95*z,c2=2.25*z,c3=3.375*z,c4=5.0625*z
  k = pixel3/imag(p2) + .0002995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.099/c, c=c1
  ELSEIF (iter==l2)
    z=-.099/c, c=c2
  ELSEIF (iter==l3)
    z=-.099/c, c=c3
  ELSEIF (iter==l4)
    z=-.099/c, c=c4
  ELSE
    z=z-.099/c
  ENDIF
  c = c + k
  z = .2*|z| + z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2548Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .4
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2548Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2514tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))-.4
  c=1/(.224+.612*real(pixel2)+flip(abs(pixel2)))-conj(.1/pixel2)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001095
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2514tna"
  periodicity = 0
  maxiter = 500
  magn = .1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2514tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2514tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))-.4
  c=1/(.224+.612*real(pixel3)+flip(abs(pixel3)))-conj(.1/pixel3)
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) + .001095
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2514tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .1
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2514tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2518tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), m4=conj(.1/pixel)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.6
  c=tanh(sinh(1.5/(.224+.612*real(pixel2)+flip(abs(pixel2)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  k2 = .15/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2518tna"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2518tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2518tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), m4=conj(.1/pixel)
  pixel3=-abs(real(@seed))+flip(imag(@seed)), m4=conj(.1/@seed)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.6
  c=tanh(sinh(1.5/(.224+.612*real(pixel3)+flip(abs(pixel3)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  k2 = .15/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2518tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2518tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2518Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), m4=conj(.1/pixel)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.6
  c=tanh(sinh(1.5/(.224+.612*real(pixel2)+flip(abs(pixel2)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  k2 = .15/pixel2
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2518Btna"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2518Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2518Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel)), m4=conj(.1/pixel)
  pixel3=-abs(real(@seed))+flip(imag(@seed)), m4=conj(.1/@seed)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.6
  c=tanh(sinh(1.5/(.224+.612*real(pixel3)+flip(abs(pixel3)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  k2 = .15/pixel3
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c - k2
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2518Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2518Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2511tna {
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=1/(real(2*pixel2-.124)+flip(imag(pixel2)*1.224))-.35
  c=1/(2.224+1.224*real(pixel2)+flip(abs(pixel2)))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) - .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2511tna"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2511tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2511tna-Julia {
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=1/(real(2*pixel2-.124)+flip(imag(pixel2)*1.224))-.35
  c=1/(2.224+1.224*real(pixel3)+flip(abs(pixel3)))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) - .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2511tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2511tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2511Btna {
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  z=1/(real(2*pixel2-.124)+flip(imag(pixel2)*1.224))-.35
  c=1/(2.224+1.224*real(pixel2)+flip(abs(pixel2)))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) - .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2511Btna"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2511Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2511Btna-Julia {
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  z=1/(real(2*pixel2-.124)+flip(imag(pixel2)*1.224))-.35
  c=1/(2.224+1.224*real(pixel3)+flip(abs(pixel3)))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) - .000995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = z^2 + c
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2511Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .08
  center = (0,0)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2511Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2517tna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  m4=conj(.1/pixel2)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.5
  c=tanh(sinh(1/(.224+.612*real(pixel2)+flip(abs(pixel2)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c + .1/(c+z)
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2517tna"
  periodicity = 0
  maxiter = 500
  magn = .1
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2517tna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2517tna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  m4=conj(.1/pixel2)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.5
  c=tanh(sinh(1/(.224+.612*real(pixel3)+flip(abs(pixel3)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c + .1/(c+z)
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2517tna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .1
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2517tna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2517Btna{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
; B is same as original - only the Julia form is different
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  m4=conj(.1/pixel2)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.5
  c=tanh(sinh(1/(.224+.612*real(pixel2)+flip(abs(pixel2)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel2/imag(p2) + .001995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c + .1/(c+z)
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2517Btna"
  periodicity = 0
  maxiter = 500
  magn = .1
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2517Btna-Julia"
  seed = #pixel
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr2517Btna-Julia{
; Updated for UF by Erik Reckase, February 2000
; Modified Sylvie Gallet frm. 
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  pixel2=-abs(real(pixel))+flip(imag(pixel))
  pixel3=-abs(real(@seed))+flip(imag(@seed))
  m4=conj(.1/pixel2)
  z=conj(sin(1/(imag(pixel2)+flip(real(pixel2-.224)*1.224))))-.5
  c=tanh(sinh(1/(.224+.612*real(pixel3)+flip(abs(pixel3)))-m4))
  c1=1.5*z, c2=2.25*z, c3=3.375*z, c4=5.0625*z
  k = pixel3/imag(p2) + .001995
  l1=round(real(p1)), l2=round(imag(p1))
  l3=round(real(p2)), l4=round(imag(p2))
  float bailout=16, int iter=0
loop:
  IF (iter==l1)
    z=-.09/c, c=c1
  ELSEIF (iter==l2)
    z=-.09/c, c=c2
  ELSEIF (iter==l3)
    z=-.09/c, c=c3
  ELSEIF (iter==l4)
    z=-.09/c, c=c4
  ELSE
    z=z-.09/c
  ENDIF
  c = c + k
  z = .2*real(z^2)*z + z^2 + c + .1/(c+z)
  iter=iter+1
bailout:
  |real(z)| <= bailout
default:
  title = "carr2517Btna-Julia"
  periodicity = 0
  maxiter = 500
  magn = .1
  center = (0,-1)
  method = multipass
  param p1
    caption = "Iter Limits 1,2"
    default = (100,150)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
  param p2
    caption = "Iter Limits 3,4"
    default = (200,250)
    hint = "0< Re(P1) < Im(P1) < Re(P2) < Im(P2) <maxiter \
            for best results"
  endparam
switch:
  type = "carr2517Btna"
  bailout = bailout
  p1 = p1
  p2 = p2
}


carr1217tna { 
; Updated for UF2 by Erik Reckase, Feb 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=(real((1.099,0))+1)/#pixel
loop:
  z=conj(cos(z/((imag((2.0,0))-1)/#pixel)))
  z=z*z+((imag((2.0,0))-1)/#pixel)
bailout:
  |real(z)| <=100
default:
  title = "carr1217tna"
  periodicity = 0
  maxiter = 500
  magn = 1
  center = (0,0)
  method = guessing
switch:
  type = "carr1217tna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1217tna-Julia{
; Updated for UF2 by Erik Reckase, Feb 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=(real((1.099,0))+1)/#pixel
loop:
  z=conj(cos(z/((imag((2.0,0))-1)/@seed)))
  z=z*z+((imag((2.0,0))-1)/@seed)
bailout:
  |real(z)| <=100
default:
  title = "carr1217tna-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "carr1217tna"
  bailout = bailout
}


carr1569tna {
; carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; updated for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=z*z+(cos(pixel))/(conj(3/pixel)^conj(cos(pixel)))
bailout:
  |real(z)| <=4
default:
  title = "carr1569tna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
}


carr1569Btna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=z*z+#pixel
bailout:
  |real(z)| <=4
default:
  title = "carr1569Btna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Btna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Btna-Julia {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=z*z+@seed
bailout:
  |real(z)| <=4
default:
  title = "carr1569Btna-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "carr1569Btna"
  bailout = bailout
}


carr1569Ctna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(z)+#pixel
bailout:
  |real(z)| <=4
default:
  title = "carr1569Ctna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Ctna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Ctna-Julia {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(z)+@seed
bailout:
  |real(z)| <=4
default:
  title = "carr1569Ctna-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "carr1569Ctna"
  bailout = bailout
}


carr1569Dtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(1/z)+#pixel^2
bailout:
  |real(z)|<=4
default:
  title = "carr1569Dtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Dtna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Dtna-Julia {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(1/z)+@seed^2
bailout:
  |real(z)| <=4
default:
  title = "carr1569Dtna-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "carr1569Dtna"
  bailout = bailout
}


carr1569DDtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(1/z)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569DDtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569DDtna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569DDtna-Julia {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(1/z)+@seed
bailout:
  |real(z)|<=4
default:
  title = "carr1569DDtna-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "carr1569DDtna"
  bailout = bailout
}


carr1569DDDtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/conj(pixel)))
loop:
  z=(z^2)*cos(1/z)+#pixel^2
bailout:
  |real(z)|<=4
default:
  title = "carr1569DDDtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569DDDtna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569DDDDtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/conj(pixel)*0.88/pixel))
loop:
  z=(z^2)*cos(1/z)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569DDDDtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569DDDDtna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569DDDDDtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(1/z)+conj(#pixel)
bailout:
  |real(z)|<=4
default:
  title = "carr1569DDDDDtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569DDDDDtna-Julia"
  seed = #pixel
  bailout = bailout
}

carr1569Etna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(tan(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*tan(1/z)+#pixel^2
bailout:
  |real(z)|<=4
default:
  title = "carr1569Etna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Etna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Ftna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(sin(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*tan(1/z)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Ftna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Ftna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Gtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(sin(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(1/z^2)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Gtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Gtna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Htna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(cos(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*cos(1/z)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Htna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Htna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Itna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(cos(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*sin(1/z)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Itna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Itna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Jtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(cos(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*tan(1/z)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Jtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Jtna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Ktna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(cos(0.91/pixel*0.88/pixel))
loop:
  z=(z^2)*tan(1/z^2)+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Ktna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Ktna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Ltna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(sin(0.91/pixel*0.88/pixel))
loop:
  z=cos(z)/z+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Ltna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Ltna-Julia"
  seed = #pixel
  bailout = bailout
}


carr1569Mtna {
; from carr1569 - updated for UF2 by Erik Reckase, Jan 2000
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z=1/conj(sin(0.91/pixel*0.88/conj(pixel)))
loop:
  z=cos(z)/z+#pixel
bailout:
  |real(z)|<=4
default:
  title = "carr1569Mtna"
  periodicity = 0
  maxiter = 500
  magn = 1.3
  center = (0,0)
  method = multipass
switch:
  type = "carr1569Mtna-Julia"
  seed = #pixel
  bailout = bailout
}


amber {
; originally bwp26
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z = #pixel
loop:
  a = z ^ 3 + (#pixel - 1) * z - #pixel
  b = @Coeff1 * z ^ 2 + #pixel - 1
  z = z - @Coeff2 * a / b
bailout:
  |z| <= @bailout
default:
  title = "amber"
  param Coeff1
    caption = "Coefficient 1"
    default = (1, 0)
  endparam
  param Coeff2
    caption = "Coefficient 2"
    default = (1, 0)
  endparam
  param Bailout
    caption = "Bailout Value"
    default = 4.0
  endparam
switch:
  type = "amber-Julia"
  seed = #pixel
  bailout = bailout
  Coeff1 = Coeff1
  Coeff2 = Coeff2
}


amber-Julia{
; originally bwp26
; modified for UF3 by Ted Nason Feb 2004 archosaur@comcast.net
init:
  z = #pixel
loop:
  a = z ^ 3 + (@seed - 1) * z - @seed
  b = @Coeff1 * z ^ 2 + @seed - 1
  z = z - @Coeff2 * a / b
bailout:
  |z| <= @bailout
default:
  title = "amber-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "amber"
  Coeff1 = Coeff1
  Coeff2 = Coeff2
  bailout = bailout
}


Moth {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + real(#pixel)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + imag(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Moth"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "Moth-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


Moth-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + real(@seed)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + imag(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Moth-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "Moth"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


MothSwitch{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + imag(#pixel)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + real(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "MothSwitch"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "MothSwitch-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


MothSwitch-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + imag(@seed)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + real(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "MothSwitch-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "MothSwitch-Julia"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


MothSwitchB{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float a = 1 - real(z)*imag(#pixel)
  float dd = a^2*imag(#pixel) + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + imag(#pixel)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + real(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "MothSwitchB"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "MothSwitchB-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


MothSwitchB-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float a = 1 - real(z)*imag(@seed)
  float dd = a^2*imag(@seed) + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + imag(@seed)
  float yy = imag(z)*(@mult3*real(z)*a + @mult4*d)/dd + real(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "MothSwitchB-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 2
  endparam
  float param mult4
    caption = "multiplier 4"
    default = -1
  endparam
switch:
  type = "MothSwitchB-Julia"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
}


Storm {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(#pixel)
  float v = @mult4*real(z) / bb + imag(#pixel)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + real(#pixel)
  float yy = (@mult7*v * real(z) + @mult8*u * imag(z)) / |z| + imag(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Storm"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.05,0.05)
    hint = "Perturbation. Use (0.05,0.05) for the standard set."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 1
  endparam
  float param mult8
    caption = "multiplier 8"
    default = -1
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Storm-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Storm-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(@seed)
  float v = @mult4*real(z) / bb + imag(@seed)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + real(@seed)
  float yy = (@mult7*v * real(z) + @mult8*u * imag(z)) / |z| + imag(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Storm-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 1
  endparam
  float param mult8
    caption = "multiplier 8"
    default = -1
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Storm"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Stormswitch {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(#pixel)
  float v = @mult4*real(z) / bb + imag(#pixel)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + imag(#pixel)
  float yy = (@mult7*v * real(z) + @mult8*u * imag(z)) / |z| + real(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Stormswitch"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.05,0.05)
    hint = "Perturbation. Use (0.05,0.05) for the standard set."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 1
  endparam
  float param mult8
    caption = "multiplier 8"
    default = -1
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Stormswitch-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Stormswitch-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(@seed)
  float v = @mult4*real(z) / bb + imag(@seed)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + imag(@seed)
  float yy = (@mult7*v * real(z) + @mult8*u * imag(z)) / |z| + real(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Stormswitch-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 1
  endparam
  float param mult8
    caption = "multiplier 8"
    default = -1
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Stormswitch"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Moth-Storm {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(#pixel)
  float v = @mult4*real(z) / bb + imag(#pixel)
  float yy = (@mult5*v * real(z) + @mult6*u * imag(z)) / |z| + imag(#pixel)
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult7*d*a + @mult8*real(z)*imag(z)^2)/dd + real(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Moth-Storm"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.05,0.05)
    hint = "Perturbation. Use (0.05,0.05) for the standard set."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
    hint = "Same as mult7 in Storm"
  endparam
  float param mult6
    caption = "multiplier 6"
    default = -1
    hint = "Same as mult8 in Storm"
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 1
    hint = "Same as mult9 in Storm"
  endparam
  float param mult8
    caption = "multiplier 8"
    default = 2
    hint = "Same as mult10 in Storm"
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Moth-Storm-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Moth-Storm-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(@seed)
  float v = @mult4*real(z) / bb + imag(@seed)
  float yy = (@mult5*v * real(z) + @mult6*u * imag(z)) / |z| + imag(@seed)
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult7*d*a + @mult8*real(z)*imag(z)^2)/dd + real(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Moth-Storm-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.05,0.05)
    hint = "Perturbation. Use (0.05,0.05) for the standard set."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
    hint = "Same as mult7 in Storm"
  endparam
  float param mult6
    caption = "multiplier 6"
    default = -1
    hint = "Same as mult8 in Storm"
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 1
    hint = "Same as mult9 in Storm"
  endparam
  float param mult8
    caption = "multiplier 8"
    default = 2
    hint = "Same as mult10 in Storm"
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Moth-Storm"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Storm-Moth {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(#pixel)
  float v = @mult4*real(z) / bb + imag(#pixel)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + real(#pixel)
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float yy = imag(z)*(@mult7*real(z)*a + @mult8*d)/dd + imag(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Storm-Moth"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.05,0.05)
    hint = "Perturbation. Use (0.05,0.05) for the standard set."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 2
    hint = "Same as mult11 in Storm"
  endparam
  float param mult8
    caption = "multiplier 8"
    default = -1
    hint = "Same as mult12 in Storm"
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Storm-Moth-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Storm-Moth-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(@seed)
  float v = @mult4*real(z) / bb + imag(@seed)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + real(@seed)
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float yy = imag(z)*(@mult7*real(z)*a + @mult8*d)/dd + imag(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Storm-Moth-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
  float param mult7
    caption = "multiplier 7"
    default = 2
  endparam
  float param mult8
    caption = "multiplier 8"
    default = -1
  endparam
switch:
  type = "Storm-Moth"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
  mult7 = mult7
  mult8 = mult8
}


Egg-Moth {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float xx = real(#pixel*(z+(1/z)))
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float yy = imag(z)*(@mult1*real(z)*a + @mult2*d)/dd + imag(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Egg-Moth"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 10000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 2
    hint = "Same as mult3 in Moth"
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
    hint = "Same as mult4 in Moth"
  endparam
switch:
  type = "Egg-Moth-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
}


Egg-Moth-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float xx = real(@seed*(z+(1/z)))
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float yy = imag(z)*(@mult1*real(z)*a + @mult2*d)/dd + imag(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Egg-Moth-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 2
    hint = "Same as mult3 in Moth"
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
    hint = "Same as mult4 in Moth"
  endparam
switch:
  type = "Egg-Moth"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
}


Moth-Egg {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  yy = imag(#pixel*(z+(1/z)))
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + real(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Moth-Egg"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 10000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
switch:
  type = "Moth-Egg-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
}


Moth-Egg-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  yy = imag(@seed*(z+(1/z)))
  float a = 1 - real(z)
  float dd = a^2 + imag(z)^2
  float d = real(z)^2 - imag(z)^2
  float xx = (@mult1*d*a + @mult2*real(z)*imag(z)^2)/dd + real(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Moth-Egg-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 2
  endparam
switch:
  type = "Moth-Egg"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
}


Storm-Egg {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float yy = imag(#pixel*(z+(1/z)))
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(#pixel)
  float v = @mult4*real(z) / bb + imag(#pixel)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + real(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Storm-Egg"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 10000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
switch:
  type = "Storm-Egg-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


Storm-Egg-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float yy = imag(@seed*(z+(1/z)))
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(@seed)
  float v = @mult4*real(z) / bb + imag(@seed)
  float xx = (@mult5*u * real(z) + @mult6*v * imag(z)) / |z| + real(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Storm-Egg-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
switch:
  type = "Storm-Egg"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


Egg-Storm {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float xx = real(#pixel*(z+(1/z)))
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(#pixel)
  float v = @mult4*real(z) / bb + imag(#pixel)
  float yy = (@mult5*v * real(z) + @mult6*u * imag(z)) / |z| + imag(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Egg-Storm"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 10000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    hint = "Same as mult7 in Storm"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = -1
    hint = "Same as mult8 in Storm"
  endparam
switch:
  type = "Egg-Storm-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


Egg-Storm-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float xx = real(@seed*(z+(1/z)))
  float bb = 1 - |z|
  float u = (1 + @mult1*real(z)) * (1 + @mult2*real(z) + @mult3*imag(z)^2) / bb + real(@seed)
  float v = @mult4*real(z) / bb + imag(@seed)
  float yy = (@mult5*v * real(z) + @mult6*u * imag(z)) / |z| + imag(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "Egg-Storm-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = -1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = -1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    hint = "Same as mult7 in Storm"
    default = 1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = -1
    hint = "Same as mult8 in Storm"
  endparam
switch:
  type = "Egg-Storm"  
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


Egg-cos {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*(z+(1/cos(z)))
bailout:
  |z| <= @bailout
default:
  title = "Egg-cos"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cos-Julia"
  seed = #pixel
  bailout = bailout
}


Egg-cos-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*(z+(1/cos(z)))
bailout:
  |z| <= @bailout
default:
  title = "Egg-cos-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cos"  
  bailout = bailout
}


Egg-cosB {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*cos(z)*(z+(1/z))
bailout:
  |z| <= @bailout
default:
  title = "Egg-cosB"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cosB-Julia"
  seed = #pixel
  bailout = bailout
}


Egg-cosB-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*cos(z)*(z+(1/z))
bailout:
  |z| <= @bailout
default:
  title = "Egg-cosB-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cosB"
  seed = #pixel
  bailout = bailout
}


Egg-cosC {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*(cos(z)+1/z)
bailout:
  |z| <= @bailout
default:
  title = "Egg-cosC"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cosC-Julia"
  seed = #pixel
  bailout = bailout
}


Egg-cosC-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*(cos(z)+1/z)
bailout:
  |z| <= @bailout
default:
  title = "Egg-cosC-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cosC"
  bailout = bailout
}


Egg-cosD {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*(cos(z)^@power1+@const1)/z^@power2
bailout:
  |z| <= @bailout
default:
  title = "Egg-cosD"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  param power1
    caption = "Power1"
    default = (2,0)
    hint = "This defines the power in the numerator. Use (2,0) \
            for the standard set."
  endparam
  param power2
    caption = "Power2"
    default = (1,0)
    hint = "This defines the power in the denominator. Use (1,0) \
            for the standard set."
  endparam
  param const1
    caption = "Constant 1"
    default = (1,0)
    hint = "This defines the constant in the numerator. Use (1,0) \
            for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cosD-Julia"
  seed = #pixel
  bailout = bailout
}


Egg-cosD-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*(z^@power1+@const1)/z^@power2
bailout:
  |z| <= @bailout
default:
  title = "Egg-cosD-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  param power1
    caption = "Power1"
    default = (2,0)
    hint = "This defines the power in the numerator. Use (2,0) \
            for the standard set."
  endparam
  param power2
    caption = "Power2"
    default = (1,0)
    hint = "This defines the power in the denominator. Use (1,0) \
            for the standard set."
  endparam
  param const1
    caption = "Constant 1"
    default = (1,0)
    hint = "This defines the constant in the numerator. Use (1,0) \
            for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-cosD"
  bailout = bailout
  const1=const1
  power1=power1
  power2=power2

}


Egg {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*(z^@power1+@const1)/z^@power2
bailout:
  |z| <= @bailout
default:
  title = "Egg"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  param power1
    caption = "Power1"
    default = (2,0)
    hint = "This defines the power in the numerator. Use (2,0) \
            for the standard set."
  endparam
  param power2
    caption = "Power2"
    default = (1,0)
    hint = "This defines the power in the denominator. Use (1,0) \
            for the standard set."
  endparam
  param const1
    caption = "Constant 1"
    default = (1,0)
    hint = "This defines the constant in the numerator. Use (1,0) \
            for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg-Julia"
  seed = #pixel
  bailout = bailout
}


Egg-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*(z^@power1+@const1)/z^@power2
bailout:
  |z| <= @bailout
default:
  title = "Egg-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  param power1
    caption = "Power1"
    default = (2,0)
    hint = "This defines the power in the numerator. Use (2,0) \
            for the standard set."
  endparam
  param power2
    caption = "Power2"
    default = (1,0)
    hint = "This defines the power in the denominator. Use (1,0) \
            for the standard set."
  endparam
  param const1
    caption = "Constant 1"
    default = (1,0)
    hint = "This defines the constant in the numerator. Use (1,0) \
            for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Egg"
  bailout = bailout
  const1=const1
  power1=power1
  power2=power2

}


rationalA {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = z^2/(1-z) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "rationalA"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalA-Julia"
  seed = #pixel
  power = power
  bailout = bailout
}


rationalA-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = z^2/(1-z) + @seed
bailout:
  |z| <= @bailout
default:
  title = "rationalA-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalA"
  power = power
  bailout = bailout
}


rationalB {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = exp(z)/(1-z) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "rationalB"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalB-Julia"
  seed = #pixel
  power = power
  bailout = bailout
}


rationalB-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = exp(z)/(1-z) + @seed
bailout:
  |z| <= @bailout
default:
  title = "rationalB-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalB"
  power = power
  bailout = bailout
}


rationalC {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = c*(z^2+1)/(z*(z^2-1))  p.154
init:
  z = @start
loop:
  z = #pixel*(z^2+1)/(z*(z^2-1))
bailout:
  |z| <= @bailout
default:
  title = "rationalC"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalC-Julia"
  seed = #pixel
  bailout = bailout
}


rationalC-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = c*(z^2+1)/(z*(z^2-1))  p.154
init:
  z = #pixel
loop:
  z = @seed*(z^2+1)/(z*(z^2-1))
bailout:
  |z| <= @bailout
default:
  title = "rationalC-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalC"
  bailout = bailout
}


rationalD {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = not- z^4/(1-z)  p.70  p.154
init:
  z = @start
loop:
  float u = real(z)^2 - imag(z)^2
  float v = 2*real(z)*imag(z)
  float d = u^2 - v^2
  float a = 1 - u
  float dd = a ^ 2 + v ^ 2
  float xx = (d * a + 2 * u * v ^ 2) / dd + real(#pixel)
  float yy = v * (2 * u * a - d) / dd + imag(#pixel)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "rationalD"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalD-Julia"
  seed = #pixel
  bailout = bailout
}


rationalD-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = not- z^4/(1-z)  p.70  p.154
init:
  z = #pixel
loop:
  float u = real(z)^2 - imag(z)^2
  float v = 2*real(z)*imag(z)
  float d = u^2 - v^2
  float a = 1 - u
  float dd = a ^ 2 + v ^ 2
  float xx = (d * a + 2 * u * v ^ 2) / dd + real(@seed)
  float yy = v * (2 * u * a - d) / dd + imag(@seed)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "rationalD-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "rationalD"
  bailout = bailout
}


pq-split {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = (z+p)(z+iq)  p.154
init:
  z = @start
loop:
  z = (z+real(#pixel))*(z+flip(imag(#pixel)))
bailout:
  |z| <= @bailout
default:
  title = "pq-split"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "pq-split-Julia"
  seed = #pixel
  bailout = bailout
}


pq-split-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = (z+p)(z+iq)  p.154
init:
  z = #pixel
loop:
  z = (z+real(@seed))*(z+flip(imag(@seed)))
bailout:
  |z| <= @bailout
default:
  title = "pq-split-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "pq-split"
  bailout = bailout
}


pq-splitB {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = (z+pcosz)(z+iqsinz)  p.154
init:
  z = @start
loop:
  z = (z+cos(z)*real(#pixel))*(z+sin(z)*flip(imag(#pixel)))
bailout:
  |z| <= @bailout
default:
  title = "pq-splitB"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "pq-splitB-Julia"
  seed = #pixel
  bailout = bailout
}


pq-splitB-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
; z = (z+p)(z+iq)  p.154
init:
  z = #pixel
loop:
  z = (z+real(@seed))*(z+flip(imag(@seed)))
bailout:
  |z| <= @bailout
default:
  title = "pq-splitB-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "pq-splitB"
  bailout = bailout
}


parametricA {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float xx = (real(z)^2 - imag(z)^2 + real(#pixel)) / (1 - imag(#pixel))
  float yy = (2*real(z)*imag(z) + imag(#pixel)) / (2 - real(#pixel))
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "parametricA"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "parametricA-Julia"
  seed = #pixel
  bailout = bailout
}


parametricA-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float xx = (real(z)^2 - imag(z)^2 + real(@seed)) / (1 - imag(@seed))
  float yy = (2*real(z)*imag(z) + imag(@seed)) / (2 - real(@seed))
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "parametricA-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "parametricA"
  bailout = bailout
}


parametricB {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float xx = (real(z)^2 - imag(z)^2 + real(#pixel)) / (1 - imag(#pixel)^2)
  float yy = (2*real(z)*imag(z) + imag(#pixel)) / (2 - real(#pixel)^2)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "parametricB"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "parametricB-Julia"
  seed = #pixel
  bailout = bailout
}


parametricB-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float xx = (real(z)^2 - imag(z)^2 + real(@seed)) / (1 - imag(@seed)^2)
  float yy = (2*real(z)*imag(z) + imag(@seed)) / (2 - real(@seed)^2)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "parametricB-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "parametricB"
  bailout = bailout
}


parametricC {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  float xx = (real(z)^2 - imag(z)^2 + real(#pixel)) / (1 - imag(#pixel)^2)
  float yy = (2*real(z)*imag(z) - imag(#pixel)^2) / (2 - real(#pixel)^2)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "parametricC"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "parametricC-Julia"
  seed = #pixel
  bailout = bailout
}


parametricC-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  float xx = (real(z)^2 - imag(z)^2 + real(@seed)) / (1 - imag(@seed)^2)
  float yy = (2*real(z)*imag(z) - imag(@seed)^2) / (2 - real(@seedl)^2)
  z = xx + flip(yy)
bailout:
  |z| <= @bailout
default:
  title = "parametricC-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "parametricC"
  bailout = bailout
}


c*qtycosz-z {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*(cos(z) - z)
bailout:
  |z| <= @bailout
default:
  title = "c(cosz - z)"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "c*qtycosz-z-Julia"
  seed = #pixel
  bailout = bailout
}


c*qtycosz-z-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*(cos(z) - z)
bailout:
  |z| <= @bailout
default:
  title = "c(cosz - z)-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "c*qtycosz-z"
  bailout = bailout
}


ccosz/z {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*cos(z)/z
bailout:
  |z| <= @bailout
default:
  title = "ccosz/z"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "ccosz/z-Julia"
  seed = #pixel
  bailout = bailout
}


ccosz/z-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*cos(z)/z
bailout:
  |z| <= @bailout
default:
  title = "ccosz/z-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "ccosz/z"
  bailout = bailout
}


csinz/z {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*sin(z)/z
bailout:
  |z| <= @bailout
default:
  title = "csinz/z"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "csinz/z-Julia"
  seed = #pixel
  bailout = bailout
}


csinz/z-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*sin(z)/z
bailout:
  |z| <= @bailout
default:
  title = "csinz/z-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "csinz/z"
  bailout = bailout
}


ctanz/z {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*tan(z)/z
bailout:
  |z| <= @bailout
default:
  title = "ctanz/z"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "ctanz/z-Julia"
  seed = #pixel
  bailout = bailout
}


ctanz/z-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*tan(z)/z
bailout:
  |z| <= @bailout
default:
  title = "ctanz/z-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "ctanz/z"
  bailout = bailout
}


rational {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = (@mult1 + @mult2*z + @mult3*z^2)/(@mult4 + @mult5*z +@mult6*z^2) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "rational"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = -1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
switch:
  type = "rational-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


rational-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = (@mult1 + @mult2*z + @mult3*z^2)/(@mult4 + @mult5*z +@mult6*z^2) + @seed
bailout:
  |z| <= @bailout
default:
  title = "rational-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param seed
    caption = "Julia seed"
    default = (-1.25, 0)
    hint = "Use this to create many different Julia sets. You can \
            also set this value using the Switch feature."
  endparam
  param bailout
    caption = "Bailout value"
    default = 10000
    min = 1.0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = -1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
switch:
  type = "rational"
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


rational-switch {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = (@mult1 + @mult2*z + @mult3*z^2 + real(#pixel))/(@mult4 + @mult5*z +@mult6*z^2 +imag(#pixel))
bailout:
  |z| <= @bailout
default:
  title = "rational-switch"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = -1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
switch:
  type = "rational-switch-Julia"
  seed = #pixel
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


rational-switch-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = (@mult1 + @mult2*z + @mult3*z^2 + real(@seed))/(@mult4 + @mult5*z +@mult6*z^2 +imag(@seed))
bailout:
  |z| <= @bailout
default:
  title = "rational-switch-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0.1,0.1)
    hint = "Perturbation. Use (0.1,0.1)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
  float param mult1
    caption = "multiplier 1"
    default = 1
  endparam
  float param mult2
    caption = "multiplier 2"
    default = 1
  endparam
  float param mult3
    caption = "multiplier 3"
    default = 1
  endparam
  float param mult4
    caption = "multiplier 4"
    default = 2
  endparam
  float param mult5
    caption = "multiplier 5"
    default = -1
  endparam
  float param mult6
    caption = "multiplier 6"
    default = 1
  endparam
switch:
  type = "rational-switch"
  bailout = bailout
  mult1 = mult1
  mult2 = mult2
  mult3 = mult3
  mult4 = mult4
  mult5 = mult5
  mult6 = mult6
}


ccosz {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*cos(z)
bailout:
  |z| <= @bailout
default:
  title = "ccos(z)"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "ccosz-Julia"
  seed = #pixel
  bailout = bailout
}


ccosz-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*cos(z)
bailout:
  |z| <= @bailout
default:
  title = "ccos(z)-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "ccosz"
  bailout = bailout
}


cosz+c {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = cos(z) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "cos(z)+c"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "cosz+c-Julia"
  seed = #pixel
  bailout = bailout
}


cosz+c-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = cos(z) + @seed
bailout:
  |z| <= @bailout
default:
  title = "cos(z)+c-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "cosz+c"
  bailout = bailout
}


c-expz {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = #pixel*exp(z)
bailout:
  |z| <= @bailout
default:
  title = "c-exp(z)"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "c-expz-Julia"
  seed = #pixel
  bailout = bailout
}


c-expz-Julia {
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = @seed*exp(z)
bailout:
  |z| <= @bailout
default:
  title = "c-exp(z)-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0)."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 1000
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "c-expz"
  bailout = bailout
}


Ising{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = log(cosh(z)) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "Ising"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Ising-Julia"
  seed = #pixel
  bailout = bailout
}


Ising-Julia{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = log(cosh(z)) + @seed
bailout:
  |z| <= @bailout
default:
  title = "Ising-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "Ising"
  bailout = bailout
}


IsingB{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = log(#pixel*cosh(z)) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "IsingB"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "IsingB-Julia"
  seed = #pixel
  bailout = bailout
}


IsingB-Julia{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = log(@seed*cosh(z)) + @seed
bailout:
  |z| <= @bailout
default:
  title = "IsingB-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "IsingB"
  bailout = bailout
}


IsingC{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = log(#pixel*cosh(z/#pixel)) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "IsingC"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "IsingC-Julia"
  seed = #pixel
  bailout = bailout
}


IsingC-Julia{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = log(@seed*cosh(z/@seed)) + @seed
bailout:
  |z| <= @bailout
default:
  title = "IsingC-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "IsingC"
  bailout = bailout
}


IsingD{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = @start
loop:
  z = log(cosh(z/#pixel)) + #pixel
bailout:
  |z| <= @bailout
default:
  title = "IsingD"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "IsingD-Julia"
  seed = #pixel
  bailout = bailout
}


IsingD-Julia{
;  by Ted Nason 1996/2003  archosaur@comcast.net
init:
  z = #pixel
loop:
  z = log(cosh(z/@seed)) + @seed
bailout:
  |z| <= @bailout
default:
  title = "IsingD-Julia"
  center = (0, 0)
  helpfile = "http://home.comcast.net/~hilbertspace/formulas.htm"
  param start
    caption = "Starting point"
    default = (0,0)
    hint = "Perturbation. Use (0,0) for the standard set."
  endparam
  float param bailout
    caption = "Bailout value"
    default = 100
    min = 0
    hint = "Defines how soon an orbit bails out, i.e. doesn't belong \
            to the set anymore."
  endparam
switch:
  type = "IsingD"
  bailout = bailout
}
