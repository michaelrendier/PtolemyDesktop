PTOLEMY FORCED CONTEXT PRIMER — 2026-05-02B
Generated at 72% context. Next chat load this first.

════════════════════════════════════════════════════════
PROTOCOL REMINDERS
════════════════════════════════════════════════════════
- Never clone Ptolemy repo more than once per session. Cache at /home/claude/Ptolemy
- Token: [TOKEN_IN_PROTOCOL] (never reproduce outside instructions)
- GDM only when no GitHub token. Primary storage is always GitHub.
- Signature every response with timestamp and context %
- Alert at 50% context remaining, force primer at 85%

════════════════════════════════════════════════════════
ACTIVE STATE — WHAT WAS DONE THIS SESSION (2026-05-02)
════════════════════════════════════════════════════════

SERVER_SPEC.md — pushed to Ptolemy root
  - Deferred (blocked on Flutter client shape + HyperDatabase schema)
  - WebSocket transport confirmed (not UDP)
  - Two processes: Mouseion (Flask/web) + Ptolemy Worker (headless/compute)
  - Session IDs are HyperWebster labels

Callimachus v0.9 — pushed to Callimachus/v09/
  Files built and tested (all passing):
    core/charset.py       — Unicode 15.1 codepoint order, 97-char Basic Latin
                            UTF-8 wire encoding documented
                            PUBLIC_CHARSET sorted by ord() — this IS the standard
    core/hyperwebster.py  — canonical bijection, SHA-256 label as PK
                            Horner accumulation, keyboard order RETIRED
    core/lsh_datatype.py  — 12 spectral layers, _WRITABLE_MASK=0x0FC0
                            Rabies Principle: first_encountered immutable
                            Python __setattr__ + SQLite trigger + C++ const (C++ pending)
    database/hyperdatabase.py — SQLite, HW label PK, WAL mode
                            Database.py RETIRED (MySQL/SQL remnant gone)
                            Rabies trigger in schema
                            incomplete flag + index
    gallery/hypergallery.py — VectorAddress, HyperGallery, HyperIndex
                            HyperIndex now uses PUBLIC_CHARSET (keyboard order copy killed)
    acquire/acquire.py    — Free Dict + Datamuse + Wiktionary + Wikipedia
                            resume-by-default, lxml, no Claude gap-fill
    __init__.py           — Callimachus class: single entry point
                            get/put/exists/index/acquire/acquire_batch/gallery

  NOT YET DONE (v0.9 → live):
    - Callimachus/v09/ not yet wired as live Callimachus API
    - Database.py still sitting in Callimachus/ (retired but not deleted)
    - C++ canonical LSH_Datatype in Callimachus namespace (skeleton only)

Decisions locked this session:
  - .perm files → Kryptos (not Callimachus)
  - Flutter confirmed as universal client
  - Unicode 15.1 codepoint order = public charset standard
  - UTF-8 = wire encoding standard
  - LaTeX rendering → deferred to Qt layer (PtolShell/QTermWidget)
  - Server not built until Flutter client shape + HyperDatabase schema final
  - LSH_Datatype name: Lagrange Self-Adjoint Hyperindexing
    TODO: Hermitian/self-adjoint mathematical rigor proof (see below)
  - Callimachus = Secondary Core. No Face touches storage directly.
    All I/O: Face → Callimachus → HyperDatabase → JSON shards

════════════════════════════════════════════════════════
CRITICAL PATH — NEXT CHAT FOCUS
════════════════════════════════════════════════════════

ORDER OF EXECUTION:

1. PtolBus — HIGHEST PRIORITY
   Every Face is isolated. luthspell.py has PtolBusStub.
   Without PtolBus, CyclicContextBuffer, LuthSpell, Philadelphos,
   and blockchain commits are all blocked.
   Build: rotary semaphore + queue. Wire stub → real implementation.
   File: Pharos/PtolBus.py (or Modules/PtolBus.py — decide at build time)

2. Ptolemy3.py boot chain verification
   Philadelphos import path and Phila(self) boot unconfirmed after last fixes.
   Ptolemy doesn't fully launch. Hard blocker on everything interactive.
   Action: boot trace, fix AttributeError chain, confirm clean launch.

3. Pharos Settings system
   Protocol Directive 4: Modules parent category, Settings tab per module.
   Every module built without it gets retrofitted. Build once, use forever.
   Settings tabs needed (from TODO):
     buffer_size_lines, compression_model, hyperindex_method,
     blockchain_backend, priority_scheme, gc_on_fatal/gc_on_error,
     log_backend, report_to_ptolemy_severity, halt evaluation logic
   File: Pharos/settings/ (already exists as stub)

4. Callimachus v09 → live swap
   - Delete or archive Database.py
   - Callimachus/v09/ becomes the live API
   - Wire into any Face currently importing Database.py
   - Confirm acquire pipeline runs on 23 test words

5. Philadelphos / CyclicContextBuffer → Ainulindale wiring
   PTL_304: compression model stub → Philadelphos/Ainur call
   PTL_305: hyperindex stub → full octonion/Cayley-Dickson math
   Dependent on PtolBus (#1) being done first.

6. Archimedes Constants consolidation
   Constants.py / MyMath.py / math2.py / Formula.py — all have duplicate
   Constant class. One authoritative Constants.py, domain subclasses.
   Do before Archimedes expands further.

7. LSH_Datatype C++ canonical
   Python binding exists. C++ canonical in Callimachus namespace needed.
   const first_encountered at ground truth layer.
   Entry point for broader Ptolemy++ port.

════════════════════════════════════════════════════════
OPEN TODOS — MATHEMATICAL
════════════════════════════════════════════════════════
- LSH_Datatype: Hermitian/self-adjoint operator — mathematical rigor.
  Claim: Horner bijection operator equals its own adjoint over charset space.
  O Captain wants this mathematically rigorous (not just architectural claim).
  Hermitian = operator equals its conjugate transpose.
  For real-valued spaces: self-adjoint = symmetric operator.
  Requires formal proof that the bijection satisfies <Af,g> = <f,Ag>.
  DO NOT DWELL on this until O Captain asks for it specifically.
  Just flag it as open and rigorous treatment pending.

════════════════════════════════════════════════════════
ARCHITECTURE REFERENCE
════════════════════════════════════════════════════════
Faces:
  Pharos        Primary Core (runtime, bus, shell, entry point)
  Callimachus   Secondary Core (all data I/O — only entry to storage)
  Archimedes    Mathematics and science
  Alexandria    OpenGL visualization
  Anaximander   Navigation and travel
  Kryptos       Encryption (.perm files live here)
  Phaleron      Internal search
  Tesla         Device interfacing
  Mouseion      Flask website (thewanderinggod.tech)
  Ptolemy++     C++ port (performance-critical)
  Ainulindale   Prefilter/staging repo (work lands here before Ptolemy)
  Philadelphos  LLM/model experimentation face

Universal client: Flutter (Dart) — single codebase → Android/iOS/Web/Desktop
Transport: WebSocket (wss://) — private, token auth
LaTeX: flutter_math_forge or flutter_tex (KaTeX backend) in Flutter layer

NOT BUILDING YET:
  Server (blocked: Flutter shape + HyperDatabase schema)
  SMNNIP data population (blocked: v0.9 model not settled)
  Mouseion web (blocked: server)

════════════════════════════════════════════════════════
REPO STATE
════════════════════════════════════════════════════════
Last commit: 4f708d8 — Callimachus v0.9
Branch: main
Remote: github.com/michaelrendier/Ptolemy

Files pushed this session:
  SERVER_SPEC.md
  Callimachus/v09/ (11 files, 1346 lines)
  CONTEXT_PRIMER_2026-05-02.txt (loaded at session start)
  CONTEXT_PRIMER_2026-05-02B.txt (this file)

════════════════════════════════════════════════════════
PREVIOUS SESSION REFERENCE (2026-05-01 and prior)
════════════════════════════════════════════════════════
See CONTEXT_PRIMER_2026-05-02.txt for full prior state.
Key surviving items:
  - PtolShell.py: QTermWidget + MetaPrompt mode router (pushed)
  - LeftPanel.py, DropWindow.py (pushed)
  - Ptolemy3.py: focusItemChanged transparency, GlobalKeyFilter (pushed)
  - installation/build_qtermwidget.sh (pushed)
  - SIP: use for PtolCPP hot-path only, not transpiler
  - JARVIS archaeology: vispy/psutil/pyaudio/opengl/tesseract survive
    Qt4/Python2.7 entries dead. Do not migrate.
