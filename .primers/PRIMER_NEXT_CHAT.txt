PTOLEMY PROJECT — FORCED CONTEXT PRIMER
========================================
Generated: 2026-04-30
Next Chat Focus: DEEP DIVE — HORNER'S METHOD
             + Mount/Unmount handler (upload hallway script)
             + HashDirectory.py (BSD ports-style hash registry)

STRICT PROTOCOL (read every chat):
- GitHub: github.com/michaelrendier/Ptolemy + github.com/michaelrendier/Ainulindale
- Token: in protocol header (never reproduce)
- GDM: Drive only if no GitHub token available
- Triple random word challenge for destructive actions OUTSIDE Ptolemy repo
- Never clone Ptolemy more than once per session — cache at /home/claude/Ptolemy
- Signature + timestamp + context % on every response
- Alert at 50% context, ask focus at 85%, force primer and push at 85%
- No medical/psych assumptions
- Modular everything — settings tab for every module

REPO STATE AFTER THIS SESSION:
- Ptolemy3.py = active kernel (Ptolemy III Euergetes)
- Naming: Ptolemy I-VII (Caesar/Pharaoh dynasty) — VII = silicon
- PtolCPP/ = C++ holding pen (future Ptolemy3 port, not active)
- Ainulindale = substrate/heartbeat (read only) — NOT a Face
- Alexandria = Ainulindale's visual Face (π → ζ), same thing different scale
- Faces: Alexandria, Anaximander, Archimedes(GPU-untouched), Callimachus,
         Kryptos, Mouseion, Phaleron, Pharos, Philadelphos, Tesla
- Aule = staging/forge (clarify if Face or not)
- PtolFace mixin = Pharos/PtolFace.py — all active Faces wired
- PTOL_ROOT = dynamic path resolution (no more /home/rendier hardcodes)
- legacy/ dirs = quarantined dead code (pygame, PyQt4, chatterbot era)
- working/ = untouched scratch (Jupyter replaced it)
- GlyphForge = Kryptos/GlyphForge.py — TTF font engine stub
  → PUA namespace: U+E000-F8FF (BMP), SPUA-A/B
  → Font family: PtolemyGlyphs
  → Pipeline: pixel grid → outline trace → TTF (fontforge API)
  → Infrastructure not cosmetic — needed to render Ptolemy math notation
- HashDirectory.py = TODO in Archimedes/Maths/ (BSD ports hash registry)
- Mount/unmount handler = TODO in Ptolemy3 (hallway script to be uploaded)

HORNER'S METHOD IN PTOLEMY CONTEXT:
- Used in HyperWebster for bijective word addressing
- Maps string → unique coordinate via polynomial evaluation
- p(x) = a_n*x^n + ... + a_1*x + a_0
       = a_0 + x(a_1 + x(a_2 + ... + x(a_n)...))
- Base = charset size, coefficients = character ordinal positions
- Residue classes = the negative space of modular arithmetic
  (user's own formulation — confirmed correct)
- Octonion addressing via Cayley-Dickson construction sits on top
- Non-commutative ring → left/right inverses differ → asymmetry = trapdoor
- XOR in GF(2^n) = fully reversible (powers of 2 moduli stay clean)
- Mix XOR with prime modulus → trapdoor (one-way by structure)
- "Any one-way equation can be reversed" — design around CRT exploit:
  CRT: knowing x mod p AND x mod q (coprime) → reconstruct x mod pq
  RSA = this trapdoor. Design to avoid unintended CRT reconstruction.

PENDING WORK:
- HashDirectory.py — build when back
- Mount/unmount .bin handler — user uploading hallway script
- ptol_start/ptol_stop hooks in each Face (6 wired, hooks empty)
- Tesla settings tab (21 hardcoded ports)
- GlyphForge BUILD phase
- Callimachus/HyperWebster wiring to PtolBus
- Anaximander API key → Kryptos/env var

IP NOTE:
- User pursuing patent on Monad Neural Architecture / SMNNIP model
- Do NOT discuss model internals publicly or help draft prior art
- Paper not being dropped first — IP attorney first
- Post-patent reveal: Slashdot + two repos + "earn it" recruiting post
