PTOLEMY FORCED CONTEXT PRIMER — 2026-05-02
Generated at 85% context capacity. Next chat should load this first.

════════════════════════════════════════════════════════
PROTOCOL REMINDERS
════════════════════════════════════════════════════════
- Never clone Ptolemy repo more than once per session. Cache at /home/claude/Ptolemy
- Token: [TOKEN_IN_PROTOCOL] (never reproduce outside instructions)
- GDM only when no GitHub token. Primary storage is always GitHub.
- C/O = Command and Output (not "prompt and response")
- MetaPrompt = Ptolemy's ready-state input mode definition
- Signature every response with timestamp and context %

════════════════════════════════════════════════════════
ACTIVE STATE — WHAT WAS DONE THIS SESSION
════════════════════════════════════════════════════════
Bugs fixed and pushed:
  - Pharos/Interface.py: fixed LiveSpectrogram import path
  - Archimedes/SpectroSecurity/LiveSpectrogram.py: ImageItem.scale() → setTransform (pyqtgraph >=0.12)
  - Pharos/Menu.py: scene.height() cast to int for QRect
  - Ptolemy3.py: _init_ui rewritten (see below)

New files pushed to GitHub:
  - Pharos/PtolShell.py: QTermWidget + MetaPrompt mode router
      Modes: (none)=Ptolemy C/O cyan, >>>=Python3 green, $=Shell yellow, #=Root red
      Commits on Enter. QTermWidget owns all pty/display. _FallbackTerm if no QTermWidget.
  - Pharos/LeftPanel.py: 2-tab sidebar (Files dir-tree + Archimedes equation browser)
  - Pharos/DropWindow.py: desktop drop handler with repass function
  - installation/build_qtermwidget.sh: CMake+SIP build for Linux Mint Xia

Ptolemy3.py changes:
  - All scene widgets: ItemIsMovable + ItemIsSelectable (free-floating)
  - 70% transparent unfocused, 100% focused (focusItemChanged signal)
  - Windows/Meta/Menu key → raise_ptolemy() via _GlobalKeyFilter
  - Desktop drag-drop → DropWindow on empty desktop only
  - _GlobalKeyFilter class added before PtolBus

════════════════════════════════════════════════════════
DISCUSSIONS IN PROGRESS — NEEDS CONTINUATION
════════════════════════════════════════════════════════

1. SIP FOR PtolCPP
   Decision: Use SIP for PtolCPP hot-path modules only. Not a transpiler.
   .sip spec files from qtermwidget/python/ are the model for all future bindings.
   Action needed: Begin SIP binding template for first PtolCPP module.

2. LSH_Datatype (formerly SemanticWord, NOT LLM_Datatype)
   - 12 spectral layers, fixed array in C++: std::array<std::string, 12>
   - layer_writable_mask: uint16_t bitfield (AI-writable vs acquisition-only)
   - first_encountered: const — Rabies Principle enforced by SQLite trigger
   - tone_glyph: demotic invisible Unicode Tag sequence
   - C++ canonical definition in Callimachus namespace, SIP-bound to Python
   - SQLite schema ready to write

3. CALLIMACHUS / HYPERDATABASE ARCHITECTURE
   Current state:
     - hyperwebster.py: bijective base-N, 256-bit hex address. WORKS.
     - hyperwebster-hash.py: SHA-256 label (64-char stable) + payload (regeneration). WORKS.
     - hyperwebster_manifold.py: 97-dim character manifold + vocab factorization. WORKS.
     - ptolemy_ingest.py: lxml+spaCy ingestion pipeline, HyperGallery tiling. ACTIVE.
     - Database.py: still MySQL. THIS IS THE ONLY SQL REMNANT. NEEDS REPLACEMENT.
   
   Architecture decision needed:
     Callimachus = first Archivist. All data I/O pipes through Callimachus to requesting Face, then to Mouseion.
     HyperWebster label = primary key everywhere (replaces MySQL IDs).
     Storage hierarchy:
       JSON shards → acquisition source of truth (one per word, alphabetically sharded)
       SQLite → indexed query layer (HyperWebster label as PK)
       VastRepository → web ingestion entries
     Database.py replacement: HyperDatabase class wrapping SQLite with HyperWebster addressing.
     JSON used inside SQLite metadata column for variable fields only.

4. FLASK / UNIVERSAL CLIENT ARCHITECTURE
   Domain: www.thewanderinggod.tech (unassociated, server not built yet)
   
   Server stack decision:
     - Keep Flask/Mouseion for web-facing side
     - Computation server: separate FastAPI or ZeroMQ process (not Apache for bidirectional)
     - Transport: WebSocket or ZeroMQ for C/O streaming between client and server
   
   Universal Client targets (ONE CODEBASE, DEVICE-BRANCHING):
     - Web: Flask/Django/React (LaTeX compliant) 
     - Android: Kotlin/Go/Java APK
     - iOS: Swift app
     - Desktop: .exe (Windows), .bin (Linux), .dmg (macOS)
     - Branch logic: device detection at entry point → specific interface
   
   Architecture options to discuss next session:
     Option A: Flutter (Dart) — single codebase → Android/iOS/Web/Desktop. Fast.
     Option B: React Native → Android/iOS + React Web. JS ecosystem.
     Option C: Kotlin Multiplatform → Android/iOS/Desktop native.
     Option D: Go backend + thin platform clients. Fastest server-side.
   
   LaTeX compliance requirement narrows this. Discuss which option handles
   LaTeX rendering best (KaTeX for web, MathJax, native rendering on mobile).

5. SERVER ARCHITECTURE (from SERVER_dependencies.json)
   Old server was LAMP. New server needs:
     - Headless Ptolemy worker (job queue, receives Commands, streams Output)
     - Mouseion/Flask for web-facing thewanderinggod.tech
     - WebSocket bridge between desktop client and server worker
     - Google Drive still needed (was in SERVER_dependencies) for sync
   
   Components from old SERVER_dependencies still relevant:
     - git (version control, already in use)
     - openssh-server (laptop pull access, already in ptolemy_setup scripts)
     - Google Drive mount (ocamlfuse approach is outdated — rclone is current)
   
   Dead from SERVER_dependencies:
     - Apache2/PHP5/phpMyAdmin — replaced by Flask+SQLite
     - Dropbox — replaced by Drive or git
     - Synergy/QSynergy — replaced by Barrier (already in setup scripts)

6. QTERMWIDGET BUILD
   Script: installation/build_qtermwidget.sh (pushed)
   Run on your Linux Mint Xia machine after pulling repo.
   Tries CMake BUILD_PYTHON_BINDINGS=ON first, falls back to manual sipbuild.

════════════════════════════════════════════════════════
JARVIS → PTOLEMY LINEAGE NOTE
════════════════════════════════════════════════════════
JARVIS_dependencies.json is archaeology. Key surviving components:
  vispy, psutil, pyaudio, opengl, tesseract stack, SIP toolchain.
The Qt4→Python2.7 entries are dead. Do not migrate them.
JARVIS was reactive (tools responding to commands).
Ptolemy is generative (learns to produce its own tools).
The architectural pivot was correct.

════════════════════════════════════════════════════════
GEN STIRLING 10 NOTE
════════════════════════════════════════════════════════
Assigned role: rotational mapping corrections function for output production.
Basin of attraction = live attractor (convergent semantic space).
Points outside basin = extinct (no convergence = no valid output).
Multiple focal points show basin dynamically.
Directly relevant to: I/O notation (reverse evaluation), WernickeNetwork
decision boundaries, 10-spectral-layer geometry.

════════════════════════════════════════════════════════
REPO STATE
════════════════════════════════════════════════════════
Last commit: 0ffae7f — installation/build_qtermwidget.sh
Branch: main
Remote: github.com/michaelrendier/Ptolemy
