PTOLEMY — FORCED CONTEXT PRIMER
Session: 2026-04-29 → Next Chat
Focus: Get Ptolemy3.py running — sequential error fixes
================================================================================

STRICT PROTOCOL (never re-list, just follow):
- Least token responses
- Signature + context % every response
- 50% alert, 85% forced primer
- Never re-clone Ptolemy in same session (cache at /home/claude/Ptolemy)
- No medical/psych assumptions
- Modular EVERYTHING — Settings tab for every hook
- Notes mode until user says "build"

REPOS:
  Ptolemy:    github.com/michaelrendier/Ptolemy
  Ainulindale: github.com/michaelrendier/Ainulindale
  Ainulindale Drive folder: 18O-dD-_giAeUdCeyv8kylzyukIB0VFTO
  Update script: git -C $DIR fetch origin && git -C $DIR reset --hard origin/main

LSH MODEL: Lagrange Self-Adjoint Hyperindexing — no LLM/neural-network language

LAUNCH FIXES APPLIED THIS SESSION (all pushed):
  [x] PtolHolePunch → HolePunch, PtolKVM → KVMClient
  [x] setGeometry float: int() at source for x/y in Interface.py
  [x] QTimer.start() float cast
  [x] Philadelphos init order: _launch_philadelphos() before User()
  [x] Interface.py setOutput None guard
  [x] cronJob: normalize float seconds → int ms (0.01s → 10ms)

CURRENT ERROR SEQUENCE (Ptolemy3.py sequential boot errors):
  Last fixed: cronJob TypeError setInterval float
  Next expected: QGraphicsProxyWidget embed warning is non-fatal (just a warning)
                 Watch for next AttributeError or import failure after UserDisplay init

ARCHITECTURE LOCKED:
  - LuthSpell = BUS controller (root, not a Face)
  - RabiesViolation PTL_504: FATAL, NO GC
  - Error catalog: PTL_1xx BUS / 2xx Halt / 3xx Buffer / 4xx Chain /
                   5xx Acquisition / 6xx LSH / 7xx Module / 9xx System
  - Bus: IntEnum priority + lexicographic (priority, timestamp), traffic circle

STUB HOOKS NEEDING REAL IMPLEMENTATION:
  - Compression model (stub → Philadelphos/Ainur)
  - Hyperindex method (stub → octonion/Cayley-Dickson)
  - Blockchain backend (stub → JackCoin)
  - PtolBusStub → real PtolBus
  - HaltingMonitor._evaluate() → real coord evaluation
  - GC → C++ RAII (deferred Ptolemy++)

THE VIEWER (Galadriel's Mirror) — NOTES MODE:
  - Archimedes super calculator / graphing engine
  - Each scalar variable → slider
  - Constants → display only
  - Vectors/tensors/matrices → sliding rotation
  - sympy.solve() for equation reversal
  - inspect.getfullargspec() already in Misc — wire to UI
  - Docstring variable names ARE the UI field labels
  - @staticmethod decorators in Formula.py — uncomment all
  - Domain categories: 17 total (see PTOLEMY_TODO.txt)
  - Domain constants: each module owns its own (Thermo→ThermodynamicConstants etc.)

CYMATICS / HARMONIC RESONANCE (notes, not started):
  - Chladni plates, Rubens' tube, water spiral strobe, oobleck
  - Nigel Stanford as visual reference
  - RawWave.py → live audio input hook

TESLA NOTE: Coil is a COLLECTOR not transmitter. Capacitor on far side of spark gap.
  Ambient EM harvesting. "Secret" = education gap in electronics.

KEY FILES:
  Ptolemy3.py         — main entry, boot sequence
  Pharos/Interface.py — User class, UserDisplay, geometry fixes
  luthspell.py        — BUS (PtolBusStub → real)
  luthspell_error_handler.py — error catalog
  Philadelphos/cyclic_context_buffer.py — CCB
  Archimedes/Maths/Formula.py — equation engine (800 lines, static methods)
  Archimedes/Maths/GraphPlot.py — 2D/3D graphing (PyQt5)
  PTOLEMY_TODO.txt    — full inline TODO scan by file+line

================================================================================
