# requirements_acquire.txt
# HyperWebster Acquisition Pipeline — Python 3.10+ dependencies
#
# Install with:
#   pip3 install -r requirements_acquire.txt
#
# Or check/install individually:
#   python3 -m pip show requests lxml
#
# System packages (Debian/Ubuntu/Mint):
#   sudo apt install python3-lxml python3-requests
#
# Note: lxml is significantly faster than BeautifulSoup for HTML scraping.
# The lxml C extension requires libxml2 and libxslt.
# On most Debian/Ubuntu systems these are already present.
# If pip install fails on lxml, use the system package instead:
#   sudo apt install python3-lxml

# ── HTTP client ───────────────────────────────────────────────────────────────
requests>=2.31.0

# ── HTML/XML parsing (faster than BeautifulSoup) ──────────────────────────────
lxml>=4.9.0

# ── Standard library modules used (no install needed) ────────────────────────
# argparse      — CLI argument parsing
# json          — JSON serialization
# logging       — log file output
# pathlib       — path handling
# re            — regex
# time          — rate limiting delays
# random        — jitter for bot avoidance
# unicodedata   — Unicode normalization
# urllib.parse  — URL encoding
# dataclasses   — SemanticWord datatype (via LLM_Datatype_Cl.py)
# enum          — enumerations (via LLM_Datatype_Cl.py)

# ── Optional future enrichment (not required now) ────────────────────────────
# spacy>=3.7.0        — morphological analysis, NER (Phase 2 enrichment)
# nltk>=3.8.0         — corpus frequency stats, WordNet integration
# python-dotenv>=1.0  — .env key management (needed when Claude gap-fill added)
