# Stub Inventory — 2026-04-29

A pass to identify empty / near-empty `.py` files. Most candidates were
left in place because they fell into one of three categories:

  1. `__init__.py` — Python package markers, must stay empty
  2. Author stubs in active Faces — placeholders for in-progress work
  3. Architectural decisions (e.g. duplicate dirs) — not housekeeping

## Removed (7 files, all in `working/`, all empty or shebang-only)

These were verified empty, verified not imported anywhere in the codebase,
and lived inside the `working/` archive directory which is policy
read-only. Removing them does not affect any running code.

  working/SpectroSecurity/LiveWaveform.py     (0 bytes)
  working/Volkswagon/key.py                   (45 bytes — shebang only)
  working/headless/splinterbrowser.py         (0 bytes)
  working/homeworks/jeremy/tesselations.py    (67 bytes — author only)
  working/homeworks/lazluma.py                (1 byte)
  working/speechattempting.py                 (0 bytes)
  working/zork/pyzork.py                      (67 bytes — author only)

## Left in place — your stubs with intent (DO NOT auto-clean)

These look empty but carry placeholder identity for future work. They
either contain notes-as-comments or sit at named slots in the Face tree
that are reserved for you to fill in.

### Archimedes (per directive: hands off Archimedes)
  Archimedes/Actuary/Actuary.py                 (0 bytes — Face slot)
  Archimedes/Maths/LinearAlgebra.py             (66 bytes — author stub)
  Archimedes/Maths/Series/InfiniteSeries.py     (194 bytes — has math notes
    in comments: 1+1/2+1/4 → 2, harmonic series → ∞, ζ(2) = π²/6)
  Archimedes/Maths/Tests/isDivisible.py         (67 bytes — test stub)
  Archimedes/Engines/noether_engine/*/__init__.py (10 dirs — package skeleton
    for noether engine subsystems; structure is intentional)

### Other Faces — placeholders for planned work
  Phaleron/AutoTicker/AutoTicker.py             (66 bytes)
  Phaleron/Library/LibraryBook.py               (174 bytes — has notes:
    Internet Archive, Project Gutenberg as planned sources)
  Phaleron/Search/Dijkstra.ph.py                (105 bytes — notes
    "alternate name: uniform cost search")
  Tesla/HolePunchClient.py                      (65 bytes — companion to
    the working HolePunch.py)
  Tesla/HolePunchServer.py                      (73 bytes — has a broken
    `import` line, suggests work-in-progress not abandonment)
  Pharos/render_url.py                          (84 bytes — `import urllib`
    only)

## Left in place — architectural decisions (not housekeeping)

### Callimachus blockchain duplicates
  Callimachus/BlockChain/JackCoin/      (with empty Utils.py)
  Callimachus/BlockChain/JackCoin5001/  (with empty Genesis.py, Utils.py)
  Callimachus/BlockChain/JackCoin5002/  (with empty Genesis.py, Utils.py)
  Callimachus/BlockChain/JackCoin5003/  (with empty Genesis.py, Utils.py)

Four versions of the same blockchain module appear to be iteration
history. Choosing which to keep is your architectural call, not stub
cleanup. The empty files inside each version are not the issue —
the four-versions question is.

### Mouseion Django scaffolding
  Mouseion/django/feincms_app/cms/admin.py
  Mouseion/django/feincms_app/cms/tests.py
  Mouseion/django/feincms_app/cms/views.py

These are auto-generated by `django-admin startapp` with placeholder
comments ("Create your views here."). Removing them confuses Django's
app discovery if the app is ever revived. Mouseion/django/ is dormant
but the structure is intact and should stay intact.

### Mouseion/Games/PlayingCards/FixCurse.py
  69 bytes, contains a one-line shell command rather than Python code.
  Not a stub — a saved shell snippet in a `.py` file. Delete or convert
  is your call.

## Verification

Each of the 7 removed files was checked with:

  grep -rn "<filename>" --include="*.py"  # no callers found

Total impact:  7 files,  13 lines deleted, all in working/.
No imports broken, no Faces touched, no Archimedes touched.
